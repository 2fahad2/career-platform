# CLAUDE.md — دستور العمل في هذا المستودع

## المشروع
مساعد توظيف شخصي مدفوع على واتساب للسوق السعودي: يبحث يوميًا، يصفّي ببوابة خاصة بكل عميل، يولّد CV إنجليزيًا مخصصًا لكل فرصة مختارة، ويسلّمها للعميل ليقدّم بنفسه. البيع والدفع عبر سلة. المشروع الشخصي القديم (تيليجرام، tag: cef71c3) مرجع فقط — لا يُلمس ولا تُنسخ أسراره أو بياناته.

## مصدر الحقيقة — اقرأه قبل أي شيء
1. `docs/WHITEPAPER.html` (v1.0) — الورقة البيضاء الكاملة.
2. `docs/CHANGELOG-v1.1.md` — تعديلات معتمدة بعد v1.0 (تَفوق ما يخالفها في v1.0).

أي تعارض يُحل لصالحهما. قسم «الثوابت» (15) في الورقة يفوز على كل شيء. أي انحراف مطلوب: عدّل الورقة/السجل أولًا في commit مستقل بعنوان `docs: whitepaper vX.Y — <القرار>` ثم نفّذ الكود. **الوثيقة تسبق الكود دائمًا.**

## الثوابت — ملخص ملزم (التفصيل في الورقة قسم 15)
1. لا كود تقديم تلقائي إطلاقًا — لا Playwright/Selenium ولا تعبئة نماذج ولا رفع ملفات لبوابات توظيف، حتى كتجربة.
2. كل إرسال إنتاجي للعميل يمر بالبوابة بنيويًا — لا علم يتجاوزها؛ التجاوز فقط في Preview داخلي لا يرسل ولا يكتب.
3. لا كتابة في delivered ledger إلا بعد اكتمال التسليم كاملًا، والكتابة ذرية.
4. الاشتراك يُنشأ فقط عند `payment = paid` بعد إعادة تحقق من Salla API؛ معالجة الأحداث idempotent بالكامل.
5. كل ادعاء في أي CV مصدره بنك الإنجازات المؤكد فقط + احترام `forbidden_claims` حرفيًا؛ الـValidator يرفض ما خالف.
6. لا إرسال مستند قبل نجاح `validate_cv_binding` بحالة ready — ولا يُرسل شيء من الحجر أبدًا.
7. النشر الذري إلزامي لكل زوج PDF/metadata (temp → write → verify → fsync → replace).
8. كل نداء LLM مجرّد من PII — الاسم/الجوال placeholders تُحقن محليًا عند تجميع الـPDF فقط.
9. وصف الوظيفة بيانات غير موثوقة دائمًا: نموذج التقييم بلا أدوات وبلا أسرار، مخرجات مهيكلة، ولا يتحكم في إرسال أو قاعدة.
10. RLS مفعّلة وكل استعلام مقيد بالـtenant + اختبارات cross-tenant هجومية في CI.
11. الـWorker يعيد التحقق من ملكية العميل من القاعدة — لا يثق بحمولة الطابور.
12. لكل عميل/يوم حالة صادقة واحدة من: DELIVERED / NO_MATCHES / PARTIAL_DELIVERY / DISCOVERY_FAILED / CV_GENERATION_FAILED / WHATSAPP_FAILED / LEDGER_FAILED — لا نجاح صامت.
13. لا PII في السجلات ولا القناة الإدارية (رموز TEN-#### فقط) + فلاتر أسرار على كل logger.
14. النسخ الاحتياطية مشفرة، خارج السيرفر، وتُختبر بالاسترجاع دوريًا.
15. الورقة تسبق الكود.

## قرارات تقنية مقفلة
- Python 3.11+ · FastAPI · PostgreSQL 16 (+RLS) · Redis · SQLAlchemy + Alembic · Docker Compose · pytest.
- بيئتان منفصلتان تمامًا على نفس السيرفر: `staging` و`production` — compose projects وقواعد وأسرار و`.env` مستقلة. هذا السيرفر (EU) = staging + ألفا الأصدقاء فقط؛ production ينتقل لسيرفر سعودي قبل الموجة الثانية (شرط خروج).
- تخزين الملفات: نظام الملفات خلف `StorageAdapter` بواجهة S3-متوافقة، بمسار `data/tenants/<tenant_id>/...` — يُستبدل لاحقًا دون تغيير الطبقات الأعلى. الـPDFs لا تدخل قاعدة البيانات.
- كل نداءات LLM عبر Anthropic API فقط (توليد الـCV بـClaude — لا مزود آخر).
- عائلات الاستعلام تُشتق ديناميكيًا من المسارات المعتمدة في onboarding العملاء النشطين — لا قوائم تخصصات مثبتة في الكود.
- الكود والتعليقات والـcommits بالإنجليزية (conventional commits)؛ كل نصوص الواجهة للعميل بالعربية.
- الأسرار في `.env` غير متتبع + `.env.example` محدث دائمًا. ممنوع أي secret في git أو السجلات أو رسائل الخطأ.

## طريقة العمل
- اشتغل بمراحل C1→C9 كما في الورقة (قسم 13). لا تبدأ مرحلة قبل تحقق شروط خروج السابقة، وقيّمها صراحة في نهاية كل مرحلة.
- حدّث `docs/PROGRESS.md` في نهاية كل جلسة: ما أُنجز، حالة شروط الخروج، الخطوة التالية، وأي قرار معلّق على فهد.
- اختبارات إلزامية قبل اعتبار أي مرحلة مكتملة: عزل cross-tenant، idempotency للـwebhooks، binding validation، وسيناريوهات فشل تنتج الحالات الصادقة الصحيحة.
- عند الغموض: اسأل فهد ولا تفترض. إن كان القرار منتجًا أو معماريًا، اقترح نص تعديل للورقة أولًا.

## ممنوع بدون إذن صريح من فهد
- إرسال رسائل حقيقية لأي رقم غير أرقام الاختبار المعتمدة.
- أي حذف بيانات أو إسقاط جداول أو تعديل مباشر في production.
- أي عملية شراء أو دفع أو تغيير في إعدادات مزودين خارجيين.
- كشف أو نقل أي أسرار.
