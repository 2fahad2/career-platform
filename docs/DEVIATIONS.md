# DEVIATIONS — الانحرافات المعتمدة عن LEGACY_KNOWLEDGE.md

> `docs/LEGACY_KNOWLEDGE.md` مرجع هندسي **للقراءة فقط** (sha256 مثبت:
> `23295ff1c9aac8af5ffae54b2449b8961eb19a087ed09798d5a101b07e419a86`).
> كل انحراف مقصود عنه يُسجَّل هنا: القسم المرجعي، الانحراف، السبب، المعتمِد.
> ما ليس مسجلًا هنا يُنقل كما هو. عند التعارض: WHITEPAPER + CHANGELOG يفوزان
> (حوكمة المنتج)، وLEGACY يفوز في تفاصيل المنطق الهندسي المُثبت غير المتعارضة.

| # | القسم المرجعي | الانحراف | السبب | المعتمِد |
|---|---|---|---|---|
| D1 | §10 (LLM router) | مزود واحد: **Anthropic فقط** (لا OpenAI، لا ترتيب مزودين). يبقى: التدهور الآمن إلى rule-based، والمدققات كلها، وbudget guard، وmax_retries | قرار مقفل في الورقة §03: كل النداءات عبر Claude | الورقة v1.0 |
| D2 | §9 (Telegram) | قناة **العميل = WhatsApp Cloud API**؛ تيليجرام قناة **إدارية فقط** (TEN-#### بلا PII). بنية البطاقات وترتيب التسليم (card ثم CV) والحالات الصادقة تُقتبس لواتساب؛ فلتر التنقية §9.2 يُعمَّم على كل القنوات (واتساب/سلة/تيليجرام) | قرار مقفل في الورقة §03 | الورقة v1.0 |
| D3 | §8 (سجلّات JSON) | السجلّات تصبح **جداول PostgreSQL** (+RLS): كبت التسليم = `deliveries`/`tenant_job_suppressions` بـTTL 30 يومًا؛ نفس الدلالات محفوظة حرفيًا (تسجيل بعد اكتمال التسليم فقط، كتابة ذرية عبر معاملة، قراءة fail-open للكبت، برونينغ منتهي الصلاحية) | قرار تقني مقفل: PostgreSQL 16 من اليوم الأول | الورقة v1.0 |
| D4 | §2 (حد 24k) | **لا حد راتب عالمي مثبت في الكود.** يُجمع من كل عميل في الإعداد **«راتب متوقع»** (رقم مستهدف — قد يكون 7k أو 15k أو غيره)، وليس شرط قبول صارمًا: (أ) وظيفة براتب **معلن** أقل من المتوقع → تُحجب وتُسجل near-miss؛ (ب) وظيفة **بلا راتب معلن → لا تُحجب افتراضيًا** (السياسة الافتراضية «متوازنة»؛ «صارمة/واسعة» خيار للعميل) — لأن أغلب الوظائف لا تعلن الراتب وحجبها يضيّع فرصًا كثيرة. منطق الـparsing كله (annual÷12، markers عبارات، fail-closed على التناقض) يبقى حرفيًا، وبوابة الأدلة تقيس مقابل عتبة العميل المحقونة بدل 24k | فهد 2026-07-14: «مو شرط 24 — حسب كل عميل؛ نبي رقم متوقع مو شرط أساسي جدًا لأنه ممكن يضيع وظائف كثير» + الورقة (search_policies بإصدارات، الافتراضي السعودي: UNKNOWN لا تُحجب مباشرة) | فهد |
| D5 | §5.1 (الاستعلامات الـ12) | لا قوائم استعلام مثبتة. `query_families` تُشتق ديناميكيًا من مسارات العملاء النشطين المعتمدة في الإعداد | الورقة (قرار معتمد + CHANGELOG §4) | الورقة v1.1 |
| D6 | §1.9 (master_cv.json لشخص واحد) | لا ماستر CV مسبق. **الملف يُبنى لكل عميل أثناء الإعداد (C5)**: رفع سيرة + محادثة جمع بيانات بأكبر قدر ممكن → استخراج → تأكيد كل حقيقة → بنك إنجازات + ممنوعات. قواعد التطبيع في §1.9 (display_company، قاعدة Security+، حارس العربي…) تبقى حرفيًا في طبقة الـrender | فهد 2026-07-14 + الورقة C5 | فهد |
| D7 | §5.3/§6/§8.2 (حجب LinkedIn) | **LinkedIn مسموح كمصدر بيانات/وظائف بشرط: بلا تسجيل دخول إطلاقًا** (صفحات عامة فقط)، ويخضع لتدقيق شروط المصادر في C6 قبل البيع. يُحذف من قوائم الحجب في الاكتشاف/الإثراء؛ يبقى حجبه المطلق في أي طبقة تقديم (لا وجود لها أصلًا — الثابت §15.1) | فهد 2026-07-14: «عادي بشرط بدون تسجيل دخول» | فهد |
| D8 | §7.4 (اسم الملف joburl-<hex>.pdf) | **التخزين الداخلي والربط** يبقيان على الهوية القانونية (stem = joburl-<64hex>؛ فحوص validate_cv_binding بلا تغيير). **اسم الملف المعروض للعميل عند الإرسال** = `FirstName LastName - Job Title.pdf` بالإنجليزية (السيرة إنجليزية)؛ عند تصادم اسمين في نفس اليوم تُضاف الشركة: `… - Job Title - Company.pdf`. يُمرر filename وقت الإرسال فقط، لا يغيّر التخزين ولا الربط | فهد 2026-07-14 (الصيغة أُكدت 14 يوليو) | فهد |
| D9 | §11/§12 (--dry-run) | إعادة تسمية إلى `--digest-only` بدلالة واضحة + وضع Preview حقيقي (لا شبكة، لا إرسال، لا كتابة). كل علم boolean له صيغة إيقاف صريحة، ولا علم واحد يحكم أثرين | الورقة §03 (انعكس) — ونفس درس §11 | الورقة v1.0 |
| D10 | §10.6 (توجيه البرومبت لمرشح محدد) | تخصيص البرومبتات (المسارات المستهدفة، التموضع، Prefer/Demote) يُبنى ديناميكيًا من ملف كل عميل المعتمد في الإعداد — لا نص مرشح مثبت | Multi-tenant (الورقة) | الورقة v1.0 |
| D11 | §12 (أسماء الموديلات) | gpt-4o/gpt-4o-mini تُستبدل بموديلات Claude الأحدث وقت التنفيذ؛ سقف max_tokens يُرفع بما يكفي للمخرجات الطويلة (تحذير §10 نفسه) | D1 + ملاحظة الترقية في التقرير | الورقة v1.0 |
| D12 | §12 (CAPTCHA config) | لا يُنقل أي إعداد CAPTCHA إطلاقًا — خارج النطاق نهائيًا مع auto-apply | الثابت §15.1 | الورقة v1.0 |
| D13 | §5.2 (SerpApi كمصدر google_jobs) | **مزود Google Jobs يصبح SearchAPI.io بدل SerpApi** (نفس محرك google_jobs ونفس البيانات: وصف كامل غير مبتور + job_highlights + روابط تقديم متعددة — تحقق حرفي من وثائق المزودين 16 يوليو). بقية معمارية §5 كما هي: نفس الـProtocol القابل للحقن، نفس fan-out وقواعد القبول والتوجيه. مصادر مجانية إضافية مرشحة (Jooble API رسمي، JobSpy-Bayt) تُدقق وتُضاف تدريجيًا حسب تقارير التغطية | التكلفة: SerpApi ‏75$/شهر لنفس البيانات التي يوفرها SearchAPI.io بـ40$ (10k بحث مقابل حاجة ~2.7k). البديل الأرخص (DataForSEO ~5$) رُفض لأنه **لا يعيد الوصف الوظيفي** والبوابة تقيّم على نص الوصف لا المسمى (المسميات تضلل — ملاحظة فهد) | فهد 2026-07-16 |

**قرارات مثبتة إضافية (من فهد 2026-07-14):** المفاتيح تُطلب منه لكل مزود باسمه عند مرحلته (Salla→C3، Meta/WhatsApp→C4، Anthropic→C5/C7، SearchAPI.io→C6 [وصل 16 يوليو — D13]، Jooble→C6 لاحقًا)؛ auto-apply لا يُقترب منه أبدًا.
