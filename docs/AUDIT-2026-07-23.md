# 🔍 التدقيق الشامل — 23 يوليو 2026

> **نقطة استئناف**: لو انقطعت الجلسة، قول «نرجع نكمل» — الحالة أدناه تُحدَّث مع كل مرحلة.

## حالة التدقيق

- [x] المرحلة 0 — مطابقة GitHub: **مثالية** (main = origin/main، صفر غير مدفوع/متتبع، فرع واحد)
- [x] المرحلة 1 — تدقيق ٧ مناطق: **اكتمل** (الجولة الأولى، قبل انقطاع الحد)
- [x] المرحلة 2 — تحقق عدائي: اكتملت (42/42)
- [x] المرحلة 3 — التقرير النهائي أدناه: **5 حرجة + 23 كبيرة مؤكدة**
- [ ] المرحلة 4 — إصلاح المؤكد ثم تحديث GitHub الكامل ← **جارية**

## مؤكد مبدئيًا من الجولة الأولى (5 ملاحظات جدية نجت من التحقق العدائي)

1. **حاوية الـAPI في staging قديمة 6 أيام** — على هجرة 0013 بينما المضيف والقاعدة على 0017؛ استقبال الويب هوك العام يمشي على كود قديم (الخدمات الحية systemd على الكود الحالي — الفجوة تشغيلية لا انقطاع)
2. **فجوة اختبارات RLS** — 14 جدول tenant بدون اختبارات عزل هجومية + لا يوجد اختبار-فوقي يفرض RLS على كل جدول جديد
3. **career_owner مستخدم superuser** — كل بنود FORCE في الهجرات بلا أثر فعلي (superuser يتجاوز RLS)؛ تعليقات 0001 والوثائق تبالغ بالادعاء
4. **لا حارس كود يمنع pytest من قاعدة staging** — سطر env واحد خطأ = كتابة/حذف في القاعدة الحية (النظام والاختبارات على نفس المنفذ 5433!)
5. **ترتيب حداثة الأدوار في الإثراء معكوس** — `_role_end_key` تصاعدي فالأقدم انتهاءً يُختار قبل الأحدث (مُثبت بإعادة إنتاج)

## معرّف الاستئناف

- Workflow run: `wf_da15c4ea-3db`
- Script: `/root/.claude/projects/-root-career/405e2eb5-c08e-4333-b6b6-7ac444257f38/workflows/scripts/full-repo-audit-wf_da15c4ea-3db.js`
- طريقة الاستئناف: Workflow بـ scriptPath + resumeFromRunId أعلاه — المدققون المكتملون يرجعون من الكاش فورًا
- آخر كومت قبل التدقيق: 49823da

## النتائج — التقرير النهائي الكامل

# تقرير الإقفال النهائي — تدقيق شامل للمنصة

## أولًا: الحكم العام

**المشروع ليس مقفلًا.** النواة الهندسية حقيقية ومثبتة حيًا — 700 اختبار تعبر بلا تخطٍّ، العزل الحي على المخطط سليم، ورحلة ملكية حقيقية اكتملت حتى التسليم الأول — لكن التدقيق العدائي أكّد 5 ملاحظات حرجة و23 كبيرة، بينها عيوب تنفيذية حية (خطر تسليم مكرر، بوابة مالية معطلة، عائلة مسار محجوبة بنيويًا) وسجل تشغيل يكذب كل ليلة؛ لا يوقّع مهندس محترف على «الإقفال» قبل معالجتها.

## ثانيًا: حالة المناطق السبع

| المنطقة | الحالة | الخلاصة الصادقة |
|---|---|---|
| قاعدة البيانات والهجرات | 🟡 | نواة صلبة ومطابقة حيًا؛ لكن حاوية النشر متأخرة 4 هجرات، وفجوات تغطية عزل، وادعاء «الدفاع في العمق» غير صحيح كما نُشر |
| واتساب والإعداد | 🔴 | آلية الإعداد والاستخلاص سليمة؛ ميزة الإثراء «الحية» فيها خطأ استهداف مثبت بالتنفيذ وخمس ثغرات، وأوامر الخصوصية ميتة كليًا لعملاء القمع |
| السيرة الذاتية والتسليم | 🔴 | متينة على مستوى الوحدات؛ لكن يوم التسليم كله معاملة واحدة (انهيار = إرسال مكرر)، وحارس الميزانية شيفرة ميتة |
| المحرك الليلي | 🔴 | النواة الحتمية وفية ومختبرة؛ لكن عائلة هندسة البرمجيات محجوبة إلى الأبد، وسجل التدقيق يكذب في كل تشغيلة |
| سلة وتيليجرام والعمليات | 🔴 | الخدمات الأربع حية ومنضبطة؛ لكن المطابقة الثلاثية معطلة في البيئة الحية، والتحويل البنكي لا يزوّد أبدًا، وعدّاد الرموز قابل لسد الطابور نهائيًا |
| الاختبارات والتكامل | 🟡 | 700 اختبار حقيقية صارمة؛ لكن حزمة الهجوم عبر المستأجرين متجمدة و14 جدولًا حساسًا بلا أي اختبار عدائي |
| التوثيق مقابل الشيفرة | 🟡 | الجوهر منفَّذ فعلًا؛ لكن قاعدة معتمدة بلا تنفيذ، وأربعة مستندات مرجعية متأخرة سباقًا كاملًا |

## ثالثًا: الملاحظات المؤكدة مرتبة بالخطورة

### الحرجة (5)

**ح-1 — يوم التسليم كله في معاملة واحدة بلا اعتماد لكل مستأجر**
src/career/engine/cli.py:222-244
الإرسال الفعلي عبر واتساب يقع في منتصف المعاملة؛ أي انهيار يفقد صفوف التسليم ودفتر الكبح وحالات اليوم لرسائل وصلت فعلًا، والنتيجة إعادة تسليم مكررة في الليلة التالية (خرق الثابتين 3 و12). ويفاقمها تأخير الكناري ساعة كاملة داخل المعاملة نفسها.
الإغلاق: اعتماد لكل مستأجر فور نجاحه، وتراجع معزول عند فشله، واختبار انهيار متعدد المستأجرين.

**ح-2 — عائلة هندسة البرمجيات محجوبة بنيويًا إلى الأبد**
src/career_core/gate.py:167
src/career/engine/gate.py:243
src/career/onboarding/paths.py:106
قائمة رفض مثبّتة في الشيفرة تُصفّر درجة المطابقة قبل الرجوع لخريطة الأدوار المشتقة من المستأجر — مستأجر مساره المعتمد «هندسة برمجيات» يُحجب عن كل وظيفة مطابقة (أُثبت بالتنفيذ الحي: شركة من الفئة الأولى وراتب صريح 30 ألفًا ➜ حجب بدرجة صفر). يناقض القرار المقفل «لا قوائم تخصصات مثبتة في الشيفرة». صفر تغطية اختبارية للعائلة عبر البوابة.
الإغلاق: إزالة الرفض المثبت أو إخضاعه لخريطة المستأجر، مع اختبارات بوابة لهذه العائلة.

**ح-3 — سجل التشغيل الليلي يكذب كل ليلة منذ 17 يوليو**
src/career/engine/cli.py:44-50,65-71,199
src/career/db/models.py:967,976
ops/engine/systemd/career-engine-nightly.service:16
علمان متناقضان افتراضيًا؛ المؤقت الإنتاجي بلا أعلام، فتُختم كل تشغيلة بأنها «ملخص فقط — لا إرسال ولا دفاتر» بينما مرحلة التسليم تعمل فعلًا في الاستدعاء نفسه (مؤكد من القاعدة: 12 تشغيلة كلها موسومة كذبًا مع تسليمات حقيقية موثقة). خرق مباشر لانضباط الأعلام في الانحراف 9 وللثابت 12، ونص المساعدة نفسه قديم.
الإغلاق: قلب الافتراض وربط العلم بالسلوك فعليًا، وتحديث النصوص، وتوثيق تصحيح السجل التاريخي.

**ح-4 — بوابة المطابقة الثلاثية في سلة معطلة في البيئة الحية**
/root/career/.env.staging
src/career/salla/provisioning.py:122
scripts/run_worker_loop.py:57
مفتاح التسعيرة غائب من ملف البيئة الحي (تحقق مباشر من بيئة العملية الحية)، فيفشل الفحص مفتوحًا: طلب مدفوع بمبلغ معدَّل أو مخفَّض يُزوَّد بأي مبلغ. البند المسمى «مقفلًا» في سجل التقدم غير صحيح حيث يهم فعلًا. والفحص نفسه مفتوح لكل منتج غائب عن التسعيرة، ويبتلع الأخطاء بصمت بلا تحذير عند الإقلاع.
الإغلاق: إضافة المفتاح للبيئة الحية، وقلب الفشل إلى مغلق (أو تحذير صارم عند الإقلاع)، واختبار يفشل عند كتالوج غير فارغ مع تسعيرة فارغة — قبل بوابة الشراء بريال.

**ح-5 — حزمة اختبارات الهجوم عبر المستأجرين متجمدة: 14 جدولًا بلا أي اختبار عدائي**
tests/test_onboarding_schema_rls.py
شرط الإقفال في البند 15-10 نصفه صحيح فقط: الجداول من الهجرات 0004 و0005 و0010 حتى 0017 — ومنها جداول الاشتراكات والتسليمات والرسائل الواردة وجدول الإثراء «الحي» — بلا اختبارات هجوم إطلاقًا، ولا يوجد اختبار فوقي على كتالوج السياسات؛ هجرة مستقبلية تنسى العزل تعبر التكامل بصمت. كما أن مسارات العفريت والليل تعمل بدور المالك المتجاوز للعزل فلا يلتقط أي اختبار تسربًا هناك. (هذه الملاحظة هي نفسها الوجه القاعدي المؤكد في منطقة قاعدة البيانات.)
الإغلاق: تمديد الحزمة المعلمية لكل جدول معزول، أو اختبار فوقي يفرض التفعيل والإجبار والسياسة على كل جدول يحمل معرف مستأجر، مع تغطية أو توثيق انحرافي لمسارات دور المالك.

### الكبيرة (23 — مجمعة بحسب المنطقة)

**قاعدة البيانات (4)**

**ك-1 — حاوية الواجهة المنشورة متأخرة 4 هجرات عن القاعدة والمستودع**
docker-compose.staging.yml
صورة عمرها 6 أيام تستقبل الويبهوكات العامة الوحيدة؛ هجراتها تنتهي عند 0013، و26 ملفًا على الأقل مختلفًا عن المضيف، ولا تستطيع حتى التحقق من المخطط. عبارة «حي عند 0017» صحيحة لخدمات المضيف فقط.
الإغلاق: إعادة بناء الصورة ونشرها، وفحص دوري يقارن رأس الهجرات داخل الحاوية بالقاعدة.

**ك-2 — فجوة تغطية العزل العدائية (الوجه القاعدي للملاحظة ح-5)**
tests/test_onboarding_schema_rls.py
الإغلاق ضمن ح-5.

**ك-3 — ادعاء «الدفاع في العمق» غير صحيح: المالك مستخدم خارق يتجاوز العزل**
migrations/versions/0001_initial_rls.py:12-13
docker-compose.staging.yml:14
كل بنود الإجبار عبر الهجرات 0001 حتى 0017 خاملة فعليًا لأن دور المالك خارق (تحقق حي)؛ والمحرك الليلي يعتمد بصمت على بقائه كذلك — لا ضبط لهوية المستأجر في المحرك إطلاقًا، وأي تقسية مستقبلية للمالك تكسر الليل كله. التوثيق يكرر الادعاء المبالغ.
الإغلاق: تصحيح التعليقات والتوثيق، وقرار صريح موثق انحرافيًا حول وضع دور المالك، مع اختبار يثبته.

**ك-4 — قاعدة «الاختبارات على قاعدة الاختبار فقط» انضباط بلا حارس برمجي**
tests/conftest.py:33
تشغيل الاختبارات بملف بيئة خاطئ يكتب ويحذف بدور خارق في قاعدة الإنتاج التجريبية نفسها — يفصل بينهما اسم القاعدة فقط على المنفذ نفسه، وبعض التنظيفات غير مقيدة النطاق، والقاعدة تحوي عميلًا حقيقيًا.
الإغلاق: تأكيد صارم في التهيئة أن اسم القاعدة ينتهي بلاحقة الاختبار وإلا فشل فوري.

**واتساب والإعداد (5)**

**ك-5 — استهداف الإثراء معكوس: يسأل عن أقدم دورين لا أحدثهما (مثبت بإعادة التنفيذ)**
src/career/onboarding/enrichment.py:101-106,136
الفرز تصاعدي بلا عكس، فيلتقط أقدم الأدوار المنتهية، والتنبيه «مرة واحدة للأبد» يُحرق نهائيًا على الدور الخطأ في المسارين معًا (الكسول والمسح). يناقض سجل التغييرات في البند 13 وسلسلة توثيق الدالة نفسها، والاختبار الوحيد ذو الصلة يقنّع الخلل.
الإغلاق: عكس الفرز واختبار ترتيب لأدوار منتهية بتواريخ متباينة.

**ك-6 — جلسة الإثراء المفتوحة تبتلع أزرار النتائج وتمنع فتح الحزمة المحجوزة حتى 72 ساعة**
src/career/whatsapp/worker.py:275-300
كل رد يذهب للإثراء قبل فرع النتائج وقبل إنزال التسليم المعلق؛ نقرة «قدمت» تُرسل لنموذج المنجزات ولا تُسجل نتيجة أبدًا، والحزمة المحجوزة تنتهي كاذبة بحالة فشل واتساب. التصادم هو التسلسل المصمم نفسه، لا فرضية. لا اختبار على مستوى العامل يغطي أيًا من التصادمين.
الإغلاق: أولوية معرفات الأزرار والتسليم المعلق قبل الإثراء، واختبارات للسلسلة كاملة عبر معالج الرسائل.

**ك-7 — أوامر الخصوصية ميتة تمامًا لعملاء تحليل السيرة (القمع) في كل مراحل حياتهم**
src/career/funnel/flow.py:149
src/career/whatsapp/worker.py:235-249
src/career/onboarding/orchestrator.py:202-208
أثناء القمع لا معالجة للأوامر (بل قد يُستهلك أمر الحذف كمسار مهني)، وبعده لا جلسة إعداد فيرجع المعالج بالرفض — «حذف بياناتي / تصدير بياناتي / وقف مؤقت / حالة اشتراكي» لا تعمل أبدًا لعميل دافع، رغم أن رسالة الترحيب نفسها تعد بها. فجوة تمس نظام حماية البيانات الشخصية ووعد «مدى الحياة» في البندين 5 و12.
الإغلاق: تفويض الأوامر الدائمة داخل مسار القمع وبعده، مع اختبارات.

**ك-8 — ضمانة «لا ترفع أبدًا» في المسح الساعي مكسورة: تنبيهات مكررة عند فشل إرسال واحد**
src/career/onboarding/enrichment.py:360-463
scripts/run_worker_loop.py:179-184
لا التقاط لكل رحلة، والاعتماد بعد المسح كله؛ فشل إرسال واحد يُرجِع صفوف «سُئل» لعملاء وصلتهم التنبيهات فعلًا، فيُعاد إرسالها في الساعة التالية (كسر ضمانة المرة الواحدة) ويُتخطى باقي المسح بصمت.
الإغلاق: التقاط لكل رحلة مع اعتماد لكل عميل قبل الإرسال التالي.

**ك-9 — اسم العميل لا يُجرَّد قبل نداء النموذج في الإثراء: خرق الثابت 8**
src/career/onboarding/enrichment.py:229-230
تمرير الاسم المعروف معدوم رغم توفره عند المستدعي، بينما مسار الاستخلاص يمرره صحيحًا؛ وسجل الانحراف 14 يدّعي زورًا حفظ الثابت. لا اختبار يغطي التجريد في الإثراء.
الإغلاق: تمرير الاسم واختبار تجريد وتصحيح سجل الانحرافات.

**السيرة الذاتية (6)**

**ك-10 — انهيار مستأجر واحد يُبتلع بلا تراجع فيسمم جلسة الجميع ويترك يومًا بلا حالة**
src/career/cv/daily_run.py:525-527
لا تراجع ولا إغلاق صادق احتياطي (خرق البند 15-12)؛ وخطأ قاعدي يفسد الجلسة المشتركة فتفشل بقية المستأجرين والاعتماد النهائي معًا — حتى الناجحون يفقدون دفاترهم رغم إرسال حقيقي. مرتبطة بالجذر نفسه في ح-1.
الإغلاق: تراجع لكل مستأجر، وحالة يوم صادقة احتياطية، واختبار عزل الانهيار.

**ك-11 — حارس ميزانية النموذج اللغوي شيفرة ميتة: لا سقف كلفة لكل مستأجر حيًا**
src/career/cv/generate.py:34-50
src/career/cv/daily_run.py:286-293
المستدعي الإنتاجي لا يمرر الميزانية أبدًا؛ مستهلكها الوحيد اختبار؛ والسقف الشهري الموعود في جدول الاستحقاقات عمود مخزن لا يقرؤه أحد. القيد الحي الوحيد ثابت 3 توليدات لكل تشغيلة.
الإغلاق: توصيل الحارس في المسار الليلي أو حذفه وتوثيق البديل صراحة.

**ك-12 — إيقاع النقاط تغير عن التوثيق بلا تعديل معتمد**
src/career/cv/enforce.py:5,28
الشيفرة 4-4-4-4 وكل السلطات المكتوبة (سلسلة توثيق الوحدة نفسها والمعرفة القديمة وسجل التقدم) تقول 4-4-3-2؛ التغيير المعتمد من فهد موجود في تعليق فقط — خرق قاعدة «التوثيق يسبق الشيفرة»، وقد يعيده صائن يحتكم للوثائق.
الإغلاق: قيد انحراف رسمي وتحديث السلسلة والوثائق.

**ك-13 — حارس تسرب العربية يفحص 3 حقول ويترك 4 حقول تُعرض فعلًا**
src/career/cv/validate.py:32-44
src/career/cv/template.py:11
العنوان الوظيفي والتعليم والشهادات واللغات تصل للقالب بلا فحص، والاستخلاص يخزن الحقائق بلغتها الأصلية عمدًا — حقيقة عربية في أي منها تتسرب للسيرة الإنجليزية بلا حجب.
الإغلاق: توسيع الفحص لكل الحقول المعروضة مع اختبار لكل حقل.

**ك-14 — مُطلِق الإثراء في مسار التسليم بلا اختبار تنسيقي ويبتلع كل استثناء**
src/career/cv/daily_run.py:368,430-431
لا اختبار يقود يوم التسليم إلى التسليح، والدالة كلها ملفوفة بابتلاع شامل؛ أي كسر توصيل يعطل المُطلِق الأساسي للميزة «الحية» بصمت تام ويحوّلها لمسح الثلاثة أيام فقط.
الإغلاق: اختبار تنسيقي من يوم تسليم حقيقي وتضييق الابتلاع.

**ك-15 — تجميع الكلفة اليومي يفوت الأيام المحجوزة والمنتهية بشكل نظامي**
src/career/cv/daily_run.py:528-536
src/career/whatsapp/worker.py:305
التجميع يُستدعى فقط للأيام المغلقة داخل التشغيلة؛ مسارا الإنزال والانتهاء لا يستدعيانه أبدًا ولا مصالحة لاحقة، فتغيب أحداث استخدام حقيقية عن جدول التخصيص المطلوب في البند 14.
الإغلاق: استدعاء التجميع في المسارين أو مصالحة ليلية لليوم السابق.

**المحرك (2)**

**ك-16 — خانة «الجديدة جدًا» في البند 6 شيفرة ميتة: تاريخ النشر لا يُخزن أبدًا**
src/career/engine/run.py:212-234,247
src/career/engine/sources.py:312,449
التاريخ الخام يُجمع من الجالبَين ثم يُهمل عند الإدراج، فتصل قيمة فارغة دائمًا في الإنتاج ولا تنطلق الخانة أبدًا؛ اختبارات الترتيب تحقن القيمة يدويًا فتقنّع الموت.
الإغلاق: تحليل التاريخ الخام وتخزينه واختبار يمر بالمسار الحقيقي.

**ك-17 — المعرفات الخام للمستأجرين تتسرب لليوميات وقناة الإدارة خلافًا للبند 15-13**
src/career/engine/run.py:390
src/career/engine/cli.py:172-174,241-248
عدّادات المستأجرين وملخص التسليم مفتاحاهما المعرف الخام لا رمز المستأجر، ويُطبعان في اليوميات ويُرسلان حرفيًا في تنبيه التشغيلة الجزئية؛ الاختبار الموجود يفحص حقلًا واحدًا فقط.
الإغلاق: ترجمة كل المفاتيح للرموز قبل الطباعة والتنبيه وتوسيع الاختبار.

**سلة وتيليجرام (2)**

**ك-18 — التحويل البنكي المؤكد من التاجر لا يزوّد أبدًا**
src/career/salla/provisioning.py:186-187,241-243
src/career/main.py:37
حدث تحديث حالة الطلب يُقبل ويُخزن ثم يُوسم «متجاهلًا» نهائيًا؛ وبحسب خريطة الدفع في الشيفرة نفسها لا يصير التحويل البنكي مدفوعًا إلا بذلك الحدث — فالمشتري بالتحويل لا يُزوَّد أبدًا وبصمت، والتحويل مفعّل في المتجر. لا اختبار يغطي المسار.
الإغلاق: معالجة الحدث تزويدًا أو إعادة فحص الطلبات الموسومة بالتجاهل، مع اختبار.

**ك-19 — توليد رموز المستأجرين بالعد يسدّ طابور سلة نهائيًا بعد أي حذف**
src/career/salla/provisioning.py:62-65
العد زائد واحد يعيد إصدار رمز مأخوذ فينفجر القيد الفريد ويبقى الحدث «مستلمًا» فيُعاد كل 3 ثوانٍ للأبد، حاجبًا كل أحداث سلة والمسحات اللاحقة في الكتلة نفسها؛ وسكربت تنظيف المستأجرين التجريبيين يفعل الحذف المفجّر بالضبط، وحجز رمز المشغل الأول غير مفروض أصلًا.
الإغلاق: تسلسل قاعدي أو أقصى رمز موجود زائد واحد، مع عزل الحدث السام بمحاولات محدودة وتنبيه.

**التوثيق (4)**

**ك-20 — قاعدة معتمدة بلا تنفيذ: مهلة السبعة أيام للمطالبة بعد الدفع شيفرة ميتة**
src/career/onboarding/policy.py:223-231
src/career/salla/lifecycle.py:90-98
الفاحص بلا مستدعٍ إنتاجي، والمسح لا يمس حالة «مدفوع غير مُطالب» أبدًا؛ المشتري الذي لا يفعّل يحتفظ باشتراك بلا ساعة للأبد ولا يصله تذكير رغم اشتراط البند 5.
الإغلاق: توصيل الفاحص في مسح دورة الحياة مع تذكير قبل المهلة واختبار.

**ك-21 — الخريطة الإنجليزية للمستودع متأخرة سباقًا كاملًا** — التفاصيل الدقيقة في القسم الخامس.
README.md

**ك-22 — الوثيقة المعمارية العربية بنفس التأخر** — التفاصيل الدقيقة في القسم الخامس.
docs/ARCHITECTURE-AR.md

**ك-23 — مرجع الحالة الإلزامي يناقض نفسه**
docs/PROGRESS.md:7,13-22,316,324
الرأس يقول آخر تحديث 15 يوليو والجلسات مسجلة حتى 20 يوليو؛ جدول المراحل يقول إن المراحل من الخامسة للتاسعة لم تبدأ بينما متون الملف نفسه تسجل إقفالها ورحلة حية كاملة؛ لا قيد لانتقال المخطط من 0013 إلى 0017؛ ونص مبهم مكرر في سطرين.
الإغلاق: تحديث الرأس والجدول وقيد الهجرات وإصلاح النص.

## رابعًا: الملاحظات الصغيرة (لم تخضع للتحقق العدائي)

**قاعدة البيانات:**
src/career/db/models.py:1090-1102
- نموذج حالة بوت الإدارة شيفرة ميتة وينقصه قيد الفحص الأحادي الموجود في الهجرة 0014 — انجراف نموذج/مخطط غير مرئي للتوليد الآلي.
migrations/env.py:30-55
- فحص الهجرات لا يقارن الافتراضات الخادمية ولا يرى سياسات العزل أو المنح أو قيود الفحص إطلاقًا — قاعدة منحرفة فيها تمر «نظيفة».
migrations/versions/0005_whatsapp_channel.py
- أعمدة معرف المستأجر بلا فهارس على جداول الرسائل الساخنة رغم أن مسند العزل يرشح بها كل استعلام.
migrations/versions/0001_initial_rls.py:78
- منح زائد: دور التطبيق يملك تعديلًا وحذفًا على جدول المستأجرين، وحذف صفه يجرف بيانات المستأجر كلها تعاقبيًا.
migrations/versions/0002_outbox_and_idempotency.py
- اسم دور التطبيق مثبت حرفيًا في كل الهجرات بينما البنية التحتية تعلمه متغيرًا — لا شيء يتحقق من تطابق الاسمين.

**واتساب والإعداد:**
src/career/onboarding/enrichment.py:39,58,86,174
- هيكل جلسات متعددة الأدوار ميت: ثابت غير مستخدم، وزر إيقاف لا يُعرض أبدًا، وطابور يُكتب ولا يُستهلك — إما التنفيذ أو الحذف.
src/career/whatsapp/client.py:230
- كل مستند يُرفع بنوع الملف المحمول حصرًا؛ ملف تصدير البيانات يصل موسومًا خطأً وغالبًا غير قابل للفتح، والمسار لم يُجرَّب حيًا قط.
src/career/whatsapp/worker.py:266,276
- نقرة تفاعلية بمعرف زر بلا عنوان نصي تتخطى فرعي الأوامر الدائمة والإثراء معًا.
src/career/onboarding/orchestrator.py:266
- الرد المقترح «نعدّي» ليس ضمن عبارات التخطي فيُرسل للنموذج كإجابة جديدة؛ ورفض الصياغة يُخلط برفض الحقيقة في قائمة الادعاءات المحظورة فتُحجب إعادة صياغة صحيحة لاحقًا.
src/career/onboarding/achievement_render.py:29-33
- ثغرة تأريض معلومة: اسم أداة بكلمة واحدة كبيرة الحرف يمر من الحارس ولا يبقى إلا تأكيد العميل على سطر إنجليزي قد لا يقرؤه بدقة.
tests/test_whatsapp_worker.py
- لا اختبار يقود سلسلة الأولويات كاملة عبر معالج الرسائل — التصادمان المؤكدان أعلاه يسكنان هذا الفراغ بالضبط، وترتيب الأدوار المنتهية غير مختبر كذلك.

**السيرة الذاتية:**
src/career/cv/daily_run.py:246-247
- عند غياب القناة تُغلق اليومية بعدّاد سير محسوم زورًا لإجبار حالة الفشل — أرقام غير صادقة تخالف روح البند 15-12.
src/career/cv/publish.py:385-387
- التعليق يقول إن التوليد الفعلي وحده يستهلك الميزانية، لكنها تُستهلك لكل نتيجة غير معادة — ثلاثة روابط بلا هوية تحجب توليدات صحيحة.
src/career/cv/daily_run.py:296-304
- الرفض بعد استهلاك الرموز لا يسجل حدث استخدام — إصلاح العد الناقص جزئي فقط.
src/career/engine/cli.py:235
- ساعة واحدة تُلتقط قبل تأخير الكناري وتُعاد لكل المستأجرين بعده — تقييم النوافذ بساعة قديمة قد يرسل نصًا حرًا في نافذة أُغلقت للتو.
src/career/cv/deliver.py:92-95
- اسم الملف الاحتياطي عند التصادم لا يُفحص هو نفسه ضد الأسماء المستخدمة — وظيفتان بنفس المسمى والشركة تنتجان اسمين متطابقين.
src/career/whatsapp/worker.py:289-305
- نقرة النتيجة تفتح النافذة لكنها لا تُنزل الحزمة المحجوزة؛ ومعرف التسليم يبقى فارغًا دائمًا في حدث النتيجة؛ واقتطاع معرف الزر عند 256 حرفًا يفسد مراجع الروابط الطويلة بصمت.
src/career/cv/generate.py:250
- مطابقة جزئية للحروف تصنف مسميات لا علاقة لها ضمن العمليات التقنية في المسار الاحتياطي — يلزم حد كلمات.
src/career/cv/publish.py:62
- تفاصيل ميتة ومنجرفة: تعبير نمطي مكرر غير مستخدم، وملف جانبي يفرّغ تحليل الوظيفة الموثق، واستيراد خاص عابر للوحدات، ومسار خطاب التغطية بلا مستدعٍ إنتاجي.

**المحرك:**
src/career_core/gate.py:25,255-260
- ثابت حد الإرسال ميت وسلسلة توثيقه مضللة — الحد الفعلي هو حد اليوم.
src/career_core/gate.py:55
- معامل الرابط في مصنّف فئات الشركات لا يُقرأ أبدًا.
src/career/engine/identity.py:47-52
- مطبّع روابط ثانٍ خاص يخالف قاعدة «لا مطبّع ثانيًا» وينسى التشذيب الأولي ويُستورد عبر الوحدات.
src/career/engine/gate.py:258
- ثلاثية شرطية بلا أي أثر — الفرعان يعيدان القيمة نفسها.
src/career/engine/ranking.py:194-196,241
- فحص «التنوع محقق طبيعيًا» كاذب دومًا بالبناء؛ الفرع ميت ويقنّعه وجود مصدرين فقط اليوم، وسينحرف عند مصدر ثالث.
src/career/engine/run.py:273-278
- وقت الانتهاء يساوي وقت البدء دائمًا؛ وأي استثناء غير معالج يمحو صف التشغيلة كليًا فلا يبقى سجل فشل — إضعاف لصدق الحالات.
src/career/engine/cli.py:137-139,187-190
- الصفر الصريح في الأعلام يعامل كغياب؛ والمفتاح السري يُمرر مرة في الرابط ومرة في الترويسة لنفس المزود — تعامل غير متسق مع سر واحد.
src/career/engine/gate.py:248-262,321-332
- لا محور أقدمية في سجل القرار ولا التقاط قربها خلافًا لسجل قرار البند 6 — انجراف شكل السجل.
src/career/engine/cli.py:94,108-125
- ترتيب مسح دورة الحياة قبل المحرك الليلي — الخاصية الوحيدة المقصودة من التوصيل — خارج التغطية كليًا بوسم استثناء التغطية.

**سلة وتيليجرام:**
src/career/engine/cli.py:118-125
- التوصيل الحي الوحيد للمسح لا يمرر عميل الإدارة، ففرع الملخص الإداري ميت إنتاجيًا؛ ومع الالتقاط الصامت حوله يمكن أن يفشل المسح كل ليلة (انتهاءات لا تُطبق) بلا أي أثر في قناة الإدارة.
src/career/telegram/views.py:42,308-321
- زر الإجراءات في القائمة الرئيسة ما زال شاشة «قريبًا» رغم أن الإجراءات شُحنت على بطاقات المستأجرين؛ وعناوين «قريبًا» تضم شاشات موجودة فعلًا — سطح نصف منجز في وحدة معلنة الإقفال.

## خامسًا: توصيات تحديث التوثيق — ما الذي قدُم بالضبط

### الخريطة الإنجليزية للمستودع
README.md
- عدد الاختبارات: السطر 363 يقول 646 والواقع 700.
- حالات اليوم: «سبع» في الأسطر 89 و221 و318 و364 والواقع ثمانٍ بعد إضافة حالة التخطي بالانسحاب في البند 15-12 من النسخة 1.1.
- جدول الهجرات (الأسطر 251-266) يتوقف عند 0014 وتنقصه 0015 و0016 و0017.
- سجل الانحرافات: السطر 295 يذكر حتى الانحراف 13 والواقع حتى 14.
- عدد الجداول المعزولة: «26» في السطرين 32 و325 والواقع 29 جدولًا معزولًا.
- السطر 112 يوثق ملفًا غير موجود في المستودع إطلاقًا (خطر تضليل عمليات النشر والشهادات):
Caddyfile
- جداول الملفات تغفل خمس وحدات موجودة:
src/career/onboarding/enrichment.py
src/career/onboarding/achievement_render.py
src/career/telegram/weekly_report.py
src/career/salla/lifecycle.py
src/career/salla/activation_link.py

### الوثيقة المعمارية العربية
docs/ARCHITECTURE-AR.md
- «646 اختبارًا» في السطر 239 والواقع 700.
- «سبع حالات» في ثلاثة مواضع (الأسطر 17 و152 و242) والواقع ثمانٍ.
- جدول الهجرات (الأسطر 186-201) ينتهي عند 0014.
- السطر 31 يوثق ملف الخادم الوسيط غير الموجود نفسه.
- جداول الملفات تغفل سبع وحدات: الخمس المذكورة أعلاه إضافة إلى:
src/career/whatsapp/activation.py
src/career/worker/worker.py

### مرجع الحالة وذاكرة العمل
docs/PROGRESS.md
- تحديث الرأس والجدول التلخيصي كما في ك-23، وإضافة قيد يوثق انتقال المخطط 0013 إلى 0017، وسحب ادعاء إقفال المطابقة الثلاثية (ليست مقفلة حيًا — انظر ح-4)، وتوثيق أيام الفشل الثلاثة المتتالية بدل عبارة «حي بالكامل».
CLAUDE.md
- تحديث عدد الاختبارات ورقم الهجرة الأخير وعدد الحالات ورقم الانحراف الأخير أينما وردت.

## خاتمة: ترتيب الأولويات قبل أي إعلان إقفال

1. ح-1 وح-4 فورًا (خطر مالي وخطر تسليم مكرر على عميل حقيقي قبل بوابة الشراء بريال).
2. ح-2 وح-3 (حجب بنيوي لعائلة معتمدة + سجل تدقيق كاذب).
3. فجوات العزل والحراسة: ح-5 مع ك-2 وك-3 وك-4 وك-1.
4. حزمة واتساب: ك-5 حتى ك-9 (ميزة الإثراء لا يجوز وصفها «حية بالكامل» قبلها) وك-18 وك-19.
5. موجة توثيق واحدة تغلق ك-12 وك-20 حتى ك-23 والقسم الخامس كله.

الصدق يقتضي القول: هذا مشروع قوي البنية بالفعل، لكنه اليوم «منطق مكتمل، توصيل وعمليات وسجل غير مكتملة» — والإقفال الحقيقي يمر عبر القائمة أعلاه لا عبر إعلانه.
