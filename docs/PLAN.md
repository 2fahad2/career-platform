# PLAN — خطة التنفيذ التفصيلية الكاملة (حتى نهاية المشروع)

> العقد التنفيذي المضاد للانحراف. الحوكمة: `WHITEPAPER.html` + `CHANGELOG-v1.1.md`
> (المنتج) · `LEGACY_KNOWLEDGE.md` (المنطق الهندسي، قراءة فقط) · `DEVIATIONS.md`
> (الانحرافات المعتمدة). **لا تُفتح مرحلة قبل تحقق شرط خروج سابقتها، ولا يُنفذ ما
> يخالف هذه الخطة قبل تعديلها في commit مستقل.**
>
> رموز: 🤖 = كلود ينفذه · 👤 = يحتاج فهد (مفاتيح/نماذج ويب/قرار/شراء).

آخر تحديث: 14 يوليو 2026 · الحالة الحالية: C1 ✅ · C2 جزئي · التالي: **P-A**

---

## خريطة الطريق

| # | المرحلة | يخدم | الحالة |
|---|---|---|---|
| P-A | النواة النقية `career_core` + اختبارات القبول | C6/C7 | ⬜ التالي |
| P-B | إغلاق C2: CI · نسخ خارجي+Restore · Queue/Outbox · Audit · فلتر أسرار | C2 | ⬜ |
| C3 | سلة: webhooks · اشتراكات · تفعيل | — | ⬜ |
| C4 | واتساب: WABA تجريبي · قوالب · تسليم تكيفي · دعم | — | ⬜ |
| C5 | الإعداد: آلة الحالات · جمع البيانات · بنك الإنجازات · المسار | — | ⬜ |
| C6 | المحرك: Pool ليلي · عائلات ديناميكية · بوابة لكل عميل | — | ⬜ |
| C7 | الـCV والتسليم: توليد Claude · ربط · نشر ذري · حالات صادقة | — | ⬜ |
| C8 | منتج تحليل الـCV | — | ⬜ |
| C9 | الموجات والقياس وتثبيت الأسعار | — | ⬜ |

قواعد كل مرحلة: تبدأ بقراءة أقسام LEGACY/الورقة المعنية · كل خطوة لها اختبار ·
compile+ruff+mypy+pytest أخضر قبل أي commit · تحديث `PROGRESS.md` وتقييم شرط
الخروج صراحة في نهايتها · commits صغيرة conventional بالإنجليزية · لا push إلا
على main بعد اخضرار كامل · لا اتصال حي بأي خدمة خارجية أول مرة إلا بإذن فهد.

---

## P-A — النواة النقية `career_core` + اختبارات القبول

**المرجع:** LEGACY §1.6, §2.1, §3, §4, §5.4, §6, §8.1 (المفاتيح), §11, §12، والملحق Verification Record.
**المبدأ:** الاختبارات أولًا (تحويل الـ120 تأكيدًا الموثقة إلى pytest)، ثم الدوال حتى تخضرّ كلها. **صفر شبكة، صفر LLM، صفر DB** — دوال نقية بإعدادات قابلة للحقن.

1. 🤖 هيكل الحزمة `src/career_core/` (نقية، بلا اعتماد على `career/`):
   `identity.py · salary.py · gate.py · sentences.py · ssrf.py · urltools.py · config.py (dataclasses للعتبات القابلة للحقن)`.
2. 🤖 `tests/acceptance/` — كتابة اختبارات القبول من الـVerification Record **قبل** أي تنفيذ:
   - هوية URL-v1: تطبيع (strip tracking بقائمة `_UTM_PARAMS` الموحدة → rstrip"/" → lower)، الاشتقاق `joburl:v1:<64hex>`، رفض غير http(s)/بلا host/فارغ، `parse/safe_stem`.
   - الراتب: annual÷12 مرة واحدة بـDecimal+ROUND_HALF_UP · k-notation لكل مبلغ · ranges قبل singles وعدم عدّ الحدود مرتين · markers عبارات فقط (حصانة "5+ years") · نافذة أمامية 15/خلفية 20 · تناقض التمثيلات (tolerance 1.0) = fail-closed · بوابة الأدلة بحالاتها الست وثقاتها — **بعتبة min_salary محقونة (D4)، لا 24k مثبتة**.
   - البوابة: tier 1/2/3/4 بالقوائم الحرفية · source-quality (90/65/20/50/0) · role-match بالرفض القاطع والأبعاد الستة (الأوزان defaults قابلة للحقن — D10) · جدول SEND/BLOCK الكامل بكل فروعه · fit-score وعتبات STRONG/GOOD/FAIR/LOW · الترتيب `(gate_order, -cq, -match)`.
   - الجمل الكاملة (§1.6): `_split_complete_sentences` (decimals، اختصارات) · `enforce_complete_summary` · `validate_summary_quality` بكل رموز الأسباب.
   - SSRF (§6): private/loopback/link-local/metadata/multicast/reserved/unspecified · فك IPv4-mapped IPv6 · فشل الحل = fail-closed · كل عنوان محلول يجب أن يكون عامًا.
   - مفاتيح الكبت: `_delivered_key` مطابق بايت-ببايت لمفتاح dedupe نفس-التشغيلة (درس P4-DUP-2).
   - انحدارات §11 كلها كاختبارات مسماة.
3. 🤖 تنفيذ الوحدات واحدة-واحدة حتى تخضرّ اختباراتها (ترتيب: urltools → identity → salary → sentences → ssrf → gate).
4. 🤖 توثيق مطابقة: تعليق في رأس كل وحدة يشير لقسم LEGACY المصدر.
5. 🤖 ruff+mypy strict نظيفان · تحديث PROGRESS · commit+push.

**شرط الخروج:** كل اختبارات القبول خضراء بلا شبكة، والحزمة مستقلة الاستيراد، وكل انحراف مسجل في DEVIATIONS.

---

## P-B — إغلاق شرط خروج C2

**المرجع:** الورقة §10/§13 (C2)، LEGACY §9.2، §13 (single-authority).

1. 🤖 **CI (GitHub Actions)** `.github/workflows/ci.yml`: على كل push/PR — services: postgres:16 (دور owner + initdb للدور التطبيقي) → ruff → mypy → alembic upgrade → pytest الكامل (storage + RLS الهجومية + قبول career_core). البادج في README.
2. 🤖 **فلتر تنقية الأسرار المعمَّم** `src/career/logging_filters.py` (نقل §9.2 حرفيًا وتعميمه: أنماط bot-URL، KV pairs، مفاتيح حساسة بالاسم، تعقيم exc_text/stack_info، يُركب على handlers) + `safe_exception_summary` + ضبط مستويات مكتبات httpx/httpcore — **قبل أول تكامل خارجي (C3) إلزامًا**.
3. 🤖 **Queue + Outbox**: جداول `outbox_events` (معاملة واحدة مع الكتابة الأصلية) و`job_queue` عبر Redis؛ كل مهمة تحمل `tenant_id · run_id · correlation_id · idempotency_key`؛ worker skeleton يعيد التحقق من الملكية من القاعدة (الثابت §15.11) — اختبار: مهمة بحمولة مزورة تُرفض.
4. 🤖 **Audit**: جدول `audit_events` (actor، action، tenant، متى، تفاصيل بلا PII) + كاتب مركزي.
5. 👤→🤖 **نسخ احتياطي مشفر خارج السيرفر + Restore**: أجهز البنية بـrestic (تشفير، جدولة، أدوار منفصلة) — **الوجهة تحتاج قرارك (سؤال Q1 أدناه)**. اختبار Restore فعلي إلى قاعدة مؤقتة وتوثيق النتيجة.
6. 🤖 اختبارات cross-tenant تتوسع لتشمل الجداول الجديدة (outbox/audit) · PROGRESS · **تقييم شرط خروج C2 كاملًا وإغلاقه**.

**شرط الخروج (الورقة):** Cross-tenant تفشل في الاختراق وتنجح في CI ✅ + Restore تجريبي ناجح من نسخة خارجية ✅.

---

## C3 — سلة: البيع والدفع والتفعيل

**المرجع:** الورقة §05, §09, §13-C3 · LEGACY لا يغطي سلة (منتج جديد).

1. 🤖 جداول: `customers · subscriptions · subscription_events · plan_entitlements · activation_tokens · webhook_events` (migration جديدة، RLS حيث يلزم).
2. 🤖 صلاحيات الباقات `plan_entitlements` seeded من جدول الورقة §04 (الأرقام قابلة للتعديل بلا كود).
3. 🤖 Webhook endpoint بالنمط الملزم حرفيًا: تحقق `X-Salla-Signature` → تسجيل الحدث بـ`event_fingerprint` → dedupe → **200 فورًا** → Worker: إعادة تحقق الطلب عبر Salla API + مطابقة (المنتج، المبلغ، العملة) → provisioning فقط عند `payment = paid` (COD معطل) → إنشاء اشتراك `PAID_UNCLAIMED` + Activation Token قصير العمر. الخطأ الممنوع: أي عمل ثقيل داخل نفس الطلب.
4. 🤖 آلة حالات الاشتراك الإحدى عشرة كاملة + مسارات الاسترداد/الإلغاء/Chargeback (تعليق فوري) + تذكير التجديد (يوم 27/29) + GRACE 48h + رسالة استرجاع بعد 7 أيام (الرسائل تُفعَّل فعليًا في C4).
5. 👤 **يدوي:** إنشاء Partner App في portal.salla.partners (أوجهك حقلًا-حقلًا) · لصق المفاتيح في `.env.staging` · إنشاء منتج الريال المخفي + هياكل المنتجات الأربعة في المتجر.
6. 🤖 اختبارات: حدث مكرر لا ينشئ اشتراكًا ثانيًا ولا يمدد مرتين (idempotency هجومي) · توقيع خاطئ يُرفض · مبلغ/منتج غير مطابق يُرفض · استرداد يعلّق فورًا.
7. 👤 شراء حقيقي بريال واحد (اختبار حي بإذنك) → 🤖 تحقق النتيجة.

**شرط الخروج (الورقة):** شراء حقيقي بريال ينشئ اشتراكًا واحدًا رغم تكرار الحدث، والاسترداد يعلّق فورًا.

---

## C4 — واتساب: القناة والتسليم التكيفي

**المرجع:** الورقة §08, §13-C4 · LEGACY §9 (بنية البطاقات وترتيب التسليم والدلالات — مكيفة لواتساب D2).

1. 👤 **يدوي:** Meta Business Portfolio + تطبيق مطور + WABA تجريبي ورقم اختبار (أوجهك خطوة-خطوة) · لصق التوكنات في `.env.staging`.
2. 🤖 تكامل Cloud API: استقبال webhook (تحقق التوقيع + dedupe) · إرسال نص/مستند/أزرار · إدارة نافذة الـ24 ساعة لكل عميل (تتبع آخر تفاعل) · Delivery callbacks → `delivery_messages`.
3. 🤖 التسليم التكيفي بالقاعدة الملزمة: نافذة مفتوحة = مباشرة؛ مقفلة = قالب صباحي واحد «لقينا لك اليوم…» → أي ضغطة تفتح فتنزل البطاقات والمستندات.
4. 🤖 القوالب: اليومي **بصياغتين** (لاختبار تصنيف Utility/Marketing) + ترحيب/تفعيل + تذكير إعداد + تذكير تجديد + استرجاع + تقرير يوم الصفر — 👤 تقديمها لمراجعة Meta من اللوحة.
5. 🤖 ربط الدفع بالرقم: زر صفحة الشكر يفتح المحادثة بتوكن التفعيل → يربط الطلب↔الرقم↔العميل؛ فشل الترحيب = تنبيه فوري لك على تيليجرام.
6. 🤖 الأوامر الدائمة الفورية: «إيقاف الرسائل» (فوري قطعي) · «دعم» → تصعيد للقناة الإدارية (تيليجرام bot admin بـTEN-#### فقط + صندوق رد).
7. 🤖 القناة الإدارية على تيليجرام مع فلتر التنقية المركب من P-B.
8. 🤖 اختبارات: التوكن يربط طلبًا برقم · منطق النافذتين · STOP يوقف فورًا · لا PII في القناة الإدارية.

**شرط الخروج (الورقة):** توكن التفعيل يربط طلبًا برقم، والتسليم التكيفي يعمل بالحالتين، وSTOP يوقف فورًا. *(الرسائل الحية على أرقام الاختبار المعتمدة فقط.)*

---

## C5 — الإعداد (Onboarding): من الدفع إلى ACTIVE

**المرجع:** الورقة §05 (تفاصيل المراحل) · LEGACY §1.9 (شكل الملف والتطبيع), §13 (فصل PII), §10.5 (variants) — مع D6 (لا ماستر مسبق).

1. 🤖 آلة الحالات العشر (`PAID_UNCLAIMED → … → ACTIVE`) قابلة للاستكمال في أي وقت + تذكير >24h + «دعم» يقاطع دائمًا.
2. 🤖 الموافقات المنفصلة بالغرض → `consent_events` بطابع زمني (تشغيل أساسي · رسائل يومية · إحصاءات مجهولة اختيارية منفصلة) + عرض الحقوق والسياسة.
3. 🤖 جمع البيانات الأساسية بأزرار وقوائم (لا استبيان 30 سؤالًا): الاسم للـCV، المدينة، المسمى، سنوات الخبرة، الإشعار، الدوام، الانتقال/عن بعد، **الحد الأدنى للراتب (سؤال Q2)**، لغة التواصل، المسار المطلوب. لا هوية ولا تاريخ ميلاد.
4. 🤖 رفع السيرة (PDF/DOCX): فحص النوع والحجم → **الفحص الأمني الكامل** (MIME، حدود صفحات، sandbox، إزالة metadata، منع JS/embedded) → استخراج النص.
5. 🤖 الاستخراج بـClaude (**مجرد من PII** — placeholders) → Draft Profile → **تأكيد كل حقيقة** (`EXTRACTED → CONFIRMED/CORRECTED/REJECTED`) بأسئلة عن الناقص والمتعارض فقط → **بنك الإنجازات** (البنية الملزمة §06 من الورقة) + `forbidden_claims`.
6. 🤖 ملف العميل بشكل §1.9 المكيّف multi-tenant (تُحفظ قواعد التطبيع الحرفية للـrender لاحقًا) — **بيانات كل عميل في جداوله tenant-scoped، لا ملفات مشتركة**.
7. 🤖 تقييم المسار بثلاث طبقات (المطلوب/المقترح/المعتمد Primary-Secondary-Stretch) بدرجة رقمية وفجوات وأقرب 3 مسارات · `customer_override` مع الإبلاغ الصريح.
8. 🤖 سياسة البحث: المسارات المعتمدة، المدن، الراتب الأدنى، سياسة «بلا راتب» (صارمة/متوازنة/واسعة)، عن بعد، القطاعات، الشركات الممنوعة، الحد اليومي من `plan_entitlements` → بطاقة ملخص → «تأكيد وبدء البحث» → ACTIVE وبداية العد (30 يومًا من إكمال الإعداد، بمهلة 7 أيام من الدفع).
9. 🤖 الأوامر الدائمة: تصدير بياناتي · حذف بياناتي · وقف مؤقت (لا يمدد) · حالة اشتراكي → `privacy_requests`.
10. 🤖 اختبارات: آلة الحالات كاملة · لا حقيقة غير مؤكدة تدخل البنك · الموافقات تُفرض قبل أي معالجة · الحذف ينفَّذ.
11. 👤 مفتاح Anthropic في `.env.staging` عند بدء هذه المرحلة.

**شرط الخروج (الورقة):** إعداد كامل من الدفع إلى ACTIVE بلا تدخل يدوي، وكل حقيقة في البنك لها حالة تأكيد.

---

## C6 — المحرك التجاري: الحلقة اليومية

**المرجع:** الورقة §06, §13-C6 · LEGACY §4, §5, §6, §8.1 عبر `career_core` (P-A) · D4, D5, D7, D10.

1. 🤖 التخطيط الليلي: اشتقاق `query_families` من اتحاد مسارات العملاء النشطين (بنية family/aliases/locations الملزمة) — ديناميكي بالكامل.
2. 🤖 الاكتشاف: SerpApi google_jobs (**المسار المباشر** — درس المسارين المنفصلين §5.2؛ تفضيل الرابط `apply_link > share_link > related_link`؛ توجيه apply_options القانوني §5.4 مع الـpromotion الآمن) + JobSpy (indeed، hours_old=336) — كل مصدر معزول بحالات مُنقّاة، أسقف في الإعدادات لا الكود.
3. 🤖 **LinkedIn (D7):** يُقيَّم ضمن تدقيق شروط المصادر — يُفعَّل كمصدر فقط إن أمكن **بلا تسجيل دخول** ونجح التدقيق؛ روابط linkedin.com في النتائج لم تعد تُحجب.
4. 🤖 الترشيح على مرحلتين: استرجاع رخيص 40–50 → إثراء 20–30 (مرة واحدة بكاش، SSRF من career_core، timeout لكل طلب) → بوابة وترتيب. Slots: شركات قوية/إعلانات عربية/تنوع مصادر/حديثة جدًا/استكشاف.
5. 🤖 الهوية الموسعة: URL-v1 أساسًا + `source_native_id · canonical_url_identity · job_entity_id · cross_source_fingerprint · repost_group_id` · **سياسة الدمج المتحفظة** (المشكوك فيه يبقى منفصلًا).
6. 🤖 بوابة كل عميل من `search_policies` (بإصدارات — لا constants): عتبة الراتب لكل عميل (D4) · سياسة UNKNOWN حسب اختياره · أوزان الدور مشتقة من مساره المعتمد (D10) → **سجل قرار لكل وظيفة** (`gate_policy_version`) + **Near-misses** لتقرير يوم الصفر.
7. 🤖 الترتيب المُصدَّر `ranking_policy_version` + Rank Trace · كبت المُرسل سابقًا على `repost_group_id` بTTL 30 يومًا (قبل قص القائمة) · قص على `daily_job_limit` للباقة.
8. 🤖 تشغيلة يومية مجدولة (systemd timer كنقطة انطلاق) بوضع `--digest-only` (D9) + Preview حقيقي.
9. 👤 مفتاح SerpApi · قرارا تدقيق المصادر النهائيان (أعرض عليك النتائج).
10. 🤖 اختبارات: عميلان بسياستين مختلفتين من Pool واحد → قائمتان مختلفتان صحيحتان · سجل قرار لكل وظيفة · الدمج المتحفظ لا يدمج المشكوك · الكبت لا يستهلك مقعدًا.

**شرط الخروج (الورقة):** تشغيلة متعددة العملاء تنتج قوائم مختلفة صحيحة لعميلين من Pool واحد، مع Rank Trace وسجل قرار لكل وظيفة.

---

## C7 — الـCV والتسليم: جوهر المنتج

**المرجع:** الورقة §06 (4–6), §13-C7 · LEGACY §1 (حرفيًا), §7 (حرفيًا), §10 (حرفيًا+D1/D10/D11), §9 (دلالات التسليم→واتساب D2) · D8.

1. 🤖 خط الـCV: قالب v5 **حرفيًا بايت-ببايت** (§1.2) + fmt_month + قواعد التطبيع الحرفية (§1.9: display_company أولًا، قاعدة Security+، حارس العربي U+0600…) + فرض الصفحة الواحدة (§1.5: 400/4/4/2/14، pacing [4,4,3,2]) + بوابة الجمل الكاملة (career_core) — ثم **فحص الشكل §1.10** ومقارنة.
2. 🤖 التوليد بـClaude فقط: برومبتات §10.1/10.2/10.3 حرفيًا (توجيه per-tenant D10) + **مدققاتها الإلزامية**: حارس المحتوى المخترع + whitelist مفردات من بنك إنجازات العميل · variants مُسبقة قبل أي LLM (§10.5) · **تجريد PII** (placeholders تُحقن محليًا عند تجميع الـPDF فقط) · شروط عدم التوليد كاملة · ميزانية توليد لكل تشغيلة.
3. 🤖 الربط والنشر: `validate_cv_binding` بقائمة فحوصه الكاملة (سلطة القبول الوحيدة) · النشر الذري للأزواج عبر StorageAdapter (temp → fsync → sha-bind → replace، `.pub-*=0`) · sidecar بالحقول الكاملة §7.3 · الحجر (لا يُرسل منه شيء أبدًا) · resolver الجيل عند-الطلب fail-closed (§7.5) · الاختيار ثنائي المرحلة hints-never-accept (§7.6).
4. 🤖 التسليم التكيفي أحد–خميس عبر C4: بطاقة (ترتيب حقول §9.1 مكيفًا) ثم **مستند الـCV مباشرة بعد بطاقته** (الربط البصري) · **اسم الملف المعروض** `<اسم العميل> - <المسمى>.pdf` (D8) · أزرار «قدّمت / تجاهل + السبب» → `outcome_events` · «CV مطلوب ولم يُحل = فشل لا تسليم».
5. 🤖 الإغلاق الصادق: لا كتابة `deliveries` إلا بعد اكتمال (بطاقة+مستند) ذريًا · الحالة اليومية الصادقة الواحدة من السبع لكل عميل · الملخص الإداري اليومي لك (TEN-####، أرقام فقط) · تتبع التكلفة `usage_events/cost_allocations` لكل عميل.
6. 🤖 اختبارات: E2E كامل لك كـTEN-0001 (دفع→إعداد→فرصة→CV→تسليم→ledger→feedback) · **كل حالة فشل مصطنعة** (فشل توليد/ربط/واتساب/ledger) تنتج الحالة الصادقة الصحيحة · لا مستند بلا ready · الحجر لا يرسل.
7. 👤 اعتماد الرسائل الحية على رقمك (canary) والمراجعة البشرية لأول CVs.

**شرط الخروج (الورقة):** E2E حي كامل لك كـTEN-0001، وكل حالة فشل مصطنعة تُنتج الحالة الصادقة الصحيحة.

---

## C8 — منتج تحليل الـCV (قمع الاكتساب)

**المرجع:** الورقة §04, §13-C8.

1. 🤖 منتج سلة مستقل رخيص + مساره في الـwebhook (يعيد استخدام بنية C3).
2. 🤖 التدفق: شراء → رفع سيرة (نفس الفحص الأمني) → اختيار المسار → تقييم عربي رقمي (درجة عامة، وضوح المسار، قوة الإنجازات، الملاءمة، جودة القراءة الآلية) + أهم 5 ملاحظات + مقارنة المسارين بدرجتيهما + 3 خطوات تالية.
3. 🤖 التسليم: PDF عربي مختصر (قابل للمشاركة) + ملخص واتساب — قالب PDF عربي جديد (RTL).
4. 🤖 الوراثة: زر ترقية → ملفه المستخرج يعبّئ بنك الإنجازات مسبقًا → إعداد نصف جاهز.
5. 🤖 اختبارات: شراء→تقرير خلال دقائق · الترقية تفتح إعدادًا نصف جاهز.

**شرط الخروج (الورقة):** شراء→رفع→تقرير خلال دقائق، والترقية تفتح إعدادًا نصف جاهز. *(يُطلق للعامة قبل الموجة الثالثة.)*

---

## C9 — الموجات والقياس وتثبيت الأسعار

**المرجع:** الورقة §14, §13-C9 · CHANGELOG §2/§3 (بوابة الموجة الثانية).

1. 🤖 الموجة 0: أنت TEN-0001 دائم — canary يمر بالدورة قبل العملاء بساعة يوميًا.
2. 👤+🤖 الموجة 1 (الصديقان): منتج الريال المخفي · onboarding كامل يدخلان بياناتهما بنفسيهما (صفر تهيئة يدوية) · حد فرصة واحدة · 10–14 يومًا · مراجعتك: الملف المستخرج + البنك + المسار + أول 3 CVs + وظائف الأسبوع الأول.
3. 🤖 مقاييس الألفا آلية (الورقة §14): الإعداد/الوظائف/الـCV/النتائج/واتساب/التشغيل + `cost_allocations` لكل عميل.
4. 👤 **بوابة الموجة الثانية (شرطا خروج إلزاميان):** الانتقال لسيرفر سعودي لبيئة production (compose الإنتاج جاهز من C2) + تجديد وثيقة العمل الحر — 🤖 أنفذ الهجرة والاستعادة عليه.
5. 👤+🤖 الموجة 2 (خمسة بسعر تجريبي حقيقي) شهر · ثم إطلاق C8 للعامة · الموجة 3 (عشرة) حتى اكتمال القياس.
6. 🤖 بعد 30 يوم قياس: تقرير الأسعار (ARPU/COGS/هامش/احتفاظ…) → 👤 تثبيت الأرقام → 🤖 رفع نسخة الورقة `docs: whitepaper vX.Y — تثبيت الأسعار` وفتح الباقات للعامة.

**شرط الخروج (الورقة):** أرقام الأسعار مثبتة في §04 بنسخة جديدة، والمنتجات الثلاثة ظاهرة في المتجر.

---

## قواعد عابرة للمراحل (تذكير دائم)

- الثوابت الخمسة عشر (§15) فوق كل شيء؛ هذه الخطة تحتها.
- الوثيقة تسبق الكود: أي انحراف عن الورقة → تعديل الورقة أولًا؛ عن LEGACY → سطر في DEVIATIONS أولًا.
- ممنوع بدون إذن صريح: رسائل لغير أرقام الاختبار · حذف بيانات/جداول · تعديل مباشر في production · أي شراء/دفع · كشف أسرار.
- كل جلسة تنتهي بتحديث PROGRESS.md (ما أُنجز، شروط الخروج، التالي، المعلّق على فهد).
