# PROJECT_STATE — لمّاح

> سجل التنفيذ والاستمرارية. يُحدَّث بعد كل موجة. **لا أسرار هنا إطلاقًا.**
> عند الاستئناف بعد انقطاع: اقرأ «أول خطوة عند الاستئناف» في آخر الملف.

## الهدف ولب المنتج

مساعد توظيف شخصي مدفوع على واتساب للسوق السعودي. اللب:

```
شراء وتفعيل → إعداد وتأكيد الحقائق → اكتشاف → بوابة لكل عميل
→ سيرة صادقة لكل فرصة → تسليم واتساب → إغلاق يوم صحيح → مراقبة واستعادة
```

**الهدف الحالي:** إغلاق الفجوة بين الكود الموجود وما يعمل فعلًا.
المبني ≈ ٨٨٪ · المثبت ≈ ٦٢٪ · الجاهز للتسليم ≈ ٤٥٪ (من تقرير ٥ أغسطس).

## خط الأساس — الموجة صفر (٥ أغسطس ٢٠٢٦)

> # ✅ سلة مربوطة بالكامل (٧ أغسطس)
>
> ```
> المتجر     1275699954 (axestore, live, private app)
> الصلاحيات   settings.read customers.read orders.read products.read_write webhooks.read_write offline_access
> التوكن      صالح حتى 2026-08-21 · مُختبَر حيًّا: GET /orders → 200, total=7
> المنتجات    471675272=29 · 1778012297=199 · 1252550546=449 · متجر https://salla.sa/axestore
> ```
>
> **ثلاثة دروس من هذا الربط، كلها من الواقع لا من الاختبارات:**
> * **حادثة DNS**: سجلّ `api` اختفى ~٢٦ ساعة (النطاق على خوادم سلة) فتوقف **كل** وارد — سلة وواتساب.
>   ولم يكن على البوابة **سجلّ زيارات إطلاقًا**، فكان «هل وصلت الرسالة؟» سؤالًا لا يُجاب إلا بالاستنتاج.
>   التغيير جاهز في `ops/caddy/` وغير منشور (`/etc` محجوب عن الأداة).
> * **العطل حمى الاعتماد**: العامل الميت منع الكود القديم من وسم حدث التوكن «متجاهَل». لو كان حيًّا لضاع.
> * **⚠️ افحص بالأداة التي يعمل بها النظام**: نداء بمكتبة بسيطة ردّ `403` مرتين (جدار سلة يحجب وكيلها)،
>   وكاد يُستنتج منه أن الاعتماد فاسد. النداء بنفس عميل الكود ردّ `200`. **سلبيّ كاذب من أداة الفحص نفسها.**
>
> # ⛔ عاجل — حادثة حيّة مفتوحة (٦ أغسطس، مؤكَّدة بالقياس على الجهاز)
>
> **`career-worker` ميت منذ ٠٦:٥٨ اليوم.** الوحدة تقول `active` و`Result=success` و`NRestarts=0`،
> والسجل فيه **٢٢٬٧٦٨ سطر خطأ**: `column webhook_events.next_attempt_at does not exist`.
> السبب: الكود أُعيد تشغيله على قاعدة عند `0020` وهو يحتاج `0022`.
>
> **الأثر المقيس**: لا معالجة واتساب ولا سلة · التشغيلة الليلية فشلت (خروج ٣) · **تسليم TEN-0002 فشل** ·
> ثلاثة أحداث محجوزة سليمة منذ ٠٨:٠٢ UTC (إشعارا تسليم + **ضغطة زر من إنسان**) — **لا شيء ضاع، لأنها صفوف**.
> وثلاثة أخطاء إقلاع تخصّ المال: توكن سلة **منتهٍ**، و`SALLA_STORE_URL` **فارغ**، ومنتج يُطابق باقة **مسحوبة من البيع**.
>
> **الإصلاح الأدنى — يحتاج إذن فهد الصريح (هجرة على قاعدة حيّة):**
> ```
> cd /root/career && .venv/bin/alembic upgrade 0022
> ```
> إضافة أعمدة فقط، بلا إسقاط ولا حذف. أما `0029` **فيُسقط جدولين** ولا يُنفَّذ إلا في بوابة النشر بإذن منفصل.
>
> **ووحدات systemd المنشورة هي نسخ ٣١ يوليو** — إصلاح `OnFailure` لم يُنشر قط، فهو غير قابل للوصول حيًّا اليوم.
>
> ---
>
> **⚠️ حالة اليوم (٦ أغسطس، بعد الموجات ٤ و٥ و٧ ومراجعتين عدائيتين)**: ~**١٥٤٠ اختبارًا** ·
> **البوابتان انفصلتا**: `scripts/gate.sh` = الشحن (أخضر) · `gate.sh --host` و`deploy_preflight.sh` = الجهاز (**أحمر بحق**) ·
> الهجرات `0021`→`0029` **مؤلَّفة ومختبَرة وغير مطبَّقة** (staging عند `0020`) ·
> **الجولة العدائية الثانية اكتملت** — سمّت انحدارين، أُصلحا في `0508695` وتحقّقتُ منهما بيدي.
> **الحالة (٦ أغسطس): الدفع تم — `origin/main` عند `0d570a2`.**
> **النشر متوقف عند خطوة محجوزة ببيئة التشغيل**: مصنّف الأذونات يرفض توجيه أداة الترحيل إلى قاعدة الإنتاج-التجريبي.
>
> **⚠️ ترتيب إلزامي — الكود يعتمد على الهجرات:** `worker.py` يستعلم عن `next_attempt_at` في مسار حيّ،
> فإعادة بناء الحاوية أو تشغيل العامل على قاعدة عند `0020` **تكسر النظام فورًا** — كل دورة استقصاء تفشل
> بعمود غير موجود. **لا يُنشر الكود قبل ترحيل القاعدة.** لذلك أُوقفت خطوات النشر عمدًا لا سهوًا.
>
> **الخطوة التالية بالضبط**: ترحيل قاعدة الإنتاج-التجريبي إلى الرأس (`0023`) — يشغّلها فهد بنفسه،
> أو تُمنح قاعدة إذن للأداة في الإعدادات. الأمر موثّق في `docs/RUNBOOK-DISASTER-RECOVERY.md`.
> ثم بالترتيب: إعادة بناء الحاوية → إعادة تشغيل العامل والبوت → التحقق أن بصمة `/health` تساوي بصمة
> المستودع → اختبارات دخان برقم الاختبار وحده → أداة استعادة الحدثين بوضع التطبيق → مراقبة السجلات
> والطوابير وحالات التسليم مدة كافية.
>
> **الأمان جاهز**: نسخة ٦ أغسطس ٠٢:٣٠ (خرجت بصفر) · آخر اختبار استعادة `PASSED` ·
> ولقطة ما قبل الترحيل داخل حاوية القاعدة: `/tmp/pre_0021_20260806_0659.dump`
>
> **الرجوع**: تنزيل الترحيل إلى `0020` (مختبَر ثلاث دورات) + إعادة بناء الصورة من الكوميت السابق.

```
(خط الأساس الأصلي، ٥ أغسطس)
HEAD            6f4390d   · main · متطابق مع origin (0 0) · شجرة نظيفة
بصمة المستودع   60b5cceca65b
بصمة الحاوية    8396389eb028   ← منحرفة (كوميت خلف)
العامل          يعمل منذ 3 أغسطس 13:23  ← خمسة كوميتات خلف
البوت           يعمل منذ 4 أغسطس 09:47
الخدمات         career-worker: active · career-admin-bot: active
المؤقتات        ثلاثة، تُطلق نفسها
البيئة          staging = إنتاج فعلي (عميل حقيقي واحد على الأقل)
```

**الأحداث المفقودة المؤكدة (معيار الاستعادة الدقيق):**

| provider | event_type | status | n | النافذة |
|---|---|---|---|---|
| whatsapp | statuses | ignored | 4 | 2026-08-03 08:05:41 → 08:05:43 |
| whatsapp | messages | failed | 1 | 2026-07-19 13:12:04 |
| salla | app.installed / app.store.authorize / app.uninstalled | ignored | 3 | 2026-07-15 — **ليست خسارة**، أحداث تركيب |

المعيار: `provider='whatsapp' AND processing_status IN ('ignored','failed')` = **٥ أحداث**.

## سجل القضايا (Issue Ledger)

الحالات المسموحة: `Implemented` · `Verified Locally` · `Verified in Isolated Env` · `Deployed` · `Live Verified` · `External Blocked`.

### P0

| # | البند | المالك | الحالة | اختبار القبول |
|---|---|---|---|---|
| P0-1 | عزل أحداث سلة عن واتساب في المكنسة | A | ✅ Verified Locally | ٣٦ اختبارًا؛ allow-list على مزوّدنا + إصلاح تجويع الـlimit + إنذار الانقطاع صار مُنطاقًا |
| P0-2 | أداة استعادة الأحداث المفقودة (dry-run افتراضي) | A | ✅ Verified Locally | `scripts/replay_lost_events.py`؛ dry-run على staging: **٢ فقط** قابلان للاستعادة من ٥ |
| P0-3 | رمز التفعيل الخام لا يظهر في تيليجرام ولا السجلات | A | ✅ Verified Locally | `issue_activation_link()` عند الطلب، TTL ٦٠ دقيقة، تدوير، ورفض لطلب مُطالَب به |
| P0-4 | زر الإيقاف/الاستئناف لا يُدخل البرج حلقة تعطّل | C | ✅ Verified Locally | ٥٧ اختبارًا تمر؛ حاجز في `handle_update` + تحقق من الحالة بعد الإجراء بدل ✅ غير مؤكدة |
| P0-5 | تطبيع عربي على موافقة القمع وأوامر الخصوصية | D | ✅ Verified Locally | ٣٣ اختبارًا؛ مصنّف بأربع نتائج + سلّم تصعيد + أزرار في القمع |
| P0-6 | تجريد PII في المسار المدفوع (ثنائي اللغة/تباين/None) | B | ✅ Verified Locally | ٢٦ اختبارًا؛ السيرة نفسها صارت مصدر اسم + مطابقة مطويّة وضبابية |
| P0-7 | رمز خروج التشغيلة يعكس فشل التسليم | E | ✅ Verified Locally | ٦٠ اختبارًا؛ `EXIT_DELIVERY_FAILED=3`؛ أي عميل يفشل = الليلة تفشل |
| P0-8 | تحقق fail-closed عند الإقلاع من إعدادات قديمة | E | ✅ Verified Locally | `verify_environment()` في نقطتي الدخول؛ يكشف الثلاثة الحقيقية اليوم |
| P0-9 | تحديث العامل والحاوية | القائد | ⏳ | بعد بوابة الموافقة فقط |
| P0-10 | `_PAUSABLE` يشمل GRACE بلا حافة | F | ✅ Verified Locally | GRACE ليست قابلة للإيقاف (§05)؛ `PAUSABLE_STATES` مشتقّة من آلة الحالات |
| P0-11 | رفض `pause/resume` لا يُميَّز عن النجاح | F + القائد | ✅ Verified Locally | `ActionOutcome` بأربع نتائج + جدولا ردود صادقة للعميل (٣٢ اختبارًا) |
| P0-12 | ترتيب `_store_offset` بعد `handle_update` | F | ✅ Verified Locally | `process_one_update` بمعاملتين؛ at-most-once مُبرَّر |
| P0-13 | «إيقاف» يُلغى إن فشل تأكيده | F | ✅ Verified Locally | commit قبل الإرسال في STOP/RESUME/SUPPORT وفرعَي النتيجة |
| P0-14 | `_handle_status` بلا حارس رتابة | F | ✅ Verified Locally | سلّم `sent<failed<delivered<read` مُبرَّر؛ اختبار على أثر الفوترة |
| P0-15 | `whatsapp/inbound.py` يطابق بلا تطبيع | G | ✅ Verified Locally | ٩٩ اختبارًا؛ انحياز مستقل لكل مجموعة؛ «وقف مؤقت» محميّة من أن تُقرأ إيقافًا |
| P0-16 | مُطلِق `issue_activation_link` في البرج | G | ✅ Verified Locally | مسار نونس التأكيد؛ الرابط في مخرَج واحد فقط، ولا في أي سجل |

### P1

| # | البند | المالك | الحالة |
|---|---|---|---|
| P1-1 | RLS حقيقية: فصل دور Runtime عن Admin + سياق مستأجر على كل مسار حي | H | 🔄 قيد التنفيذ |
| P1-2 | `webhook_events` داخل التصدير والحذف والتقليم + ربط بالمستأجر | — | ⏳ |
| P1-3 | فحص الفيروسات: تنفيذ حقيقي + health check + سياسة فشل | K + L | ✅ منطقًا (K) · 🔄 الوصل (L) | عميل clamd حقيقي؛ `scanner_health` كلّية فالبديل الصامت يُقرأ أحمر |
| P1-4 | طبقة إنذار كنظام: heartbeat + كشف تعطل + dead-letter | — | ⏳ |
| P1-5 | إيصالات ميتا: قراءة `errors[]` + حالة صادقة | — | ⏳ |
| P1-6 | `SKIPPED_OPTED_OUT` لا تمحو `DELIVERED` | J | ✅ Verified Locally | كاتب واحد لجدول حالات اليوم + اختبار AST يمنع كاتبًا ثانيًا |
| P1-7 | صندوق دعم دائم في البرج (نسخة صغرى) | G | ✅ Verified Locally | شاشة `v1|tickets` — **أول قارئ للجدول على الإطلاق** |
| P1-8 | قوالب البيئة + اختبار انحراف | J | ✅ Verified Locally | ١٨ اختبارًا؛ الانحراف يفشل في الاتجاهين؛ المجموعة مشتقّة لا مكتوبة |
| P1-9 | الوعود الثلاثة: ٧٢ ساعة · جلسة المسار · قفل السعر | — | ⏳ |
| P1-11 | لا شيء يغلق تذكرة دعم — `resolved_at` لا يُضبط في أي مكان | — | ⏳ |
| P1-12 | `normalize_ar` تسكن وحدة تجرّ `career.cv.*` — بيتها الصحيح وحدة ورقية (`career/arabic.py`) | — | ⏳ |
| P1-10 | فصل الفشل العابر عن المسموم في عامل واتساب + مسار إعادة إدراج | — | ⏳ **رفضه F صراحةً**: يحتاج هجرة (ميزانية محاولات + عمود موعد) وكانسًا — بند موجة لا بند مهمة |

### P2 — مغلقة مع K (التقرير الأسبوعي · README · تعليق ٤٩) · والحارس مع L

- التقرير الأسبوعي (`elite` موجودة، `executive` ساقطة، ثلاثة أسماء لمنتج واحد) · «49 ريالًا» في تعليق · README (٩٤٤ اختبارًا، `0019`، ٠٤:٣٠ في ستة مواضع).
- **ثغرة في حارس الأسعار نفسه**: `RETIRED` لا تحوي «49»، والاختبار يمسح `docs/` فقط لا `src/`.

### P2 — الباقي

مؤجّلة حتى إغلاق P0 وP1 — الفهارس بعد EXPLAIN · التقرير الأسبوعي · الإيراد المسترد · `cost_allocations` · الأسعار القديمة · README والورقة وPROGRESS · الأسرار في سطور الأوامر · فراغات السياسات · حذف الكود الميت.

### الأنظمة السبعة الميتة

**كلها حُسمت (٦ أغسطس).** لا يبقى منها بند مفتوح.

| النظام | ما استقرّ عليه | الحالة |
|---|---|---|
| `worker/worker.py` | حُذف. **الثابت ١١ لم يحتج نقلًا**: المداخل الحيّة لا تقرأ معرّف المستأجر من الحمولة أصلًا | ✅ |
| `audit.py` | مَوصول — أربعة أحداث، كلٌّ بمعاملته بحجّة مكتوبة (§٢٧) | ✅ |
| `quarantine_pair` | مَوصول بمسار النشر — ولا يُنقل زوج إلا إذا كانت موانعه **خصائص دائمة في الملف نفسه** | ✅ |
| `render_cover_letter_pdf` | حُذف مع حارسه في تغيير واحد، والعمود وحده باقٍ ينتظر هجرة | ✅ |
| `whatsapp/activation.py` | حُذف بعد إثبات تغطية البديل (D25) | ✅ |
| `queue/*` + outbox | حُذف، والجدولان أُسقطا في `0029` — **حادثة ٣ أغسطس استُرِدّت لأنها صفوف** | ✅ |
| `verify_restore.sh` | له وحدة ومؤقّت في المستودع — **غير مثبَّتين على الجهاز بعد** | ✅ كودًا · ⏳ تثبيتًا |

## القرارات المعمارية

- **رمز الخروج**: أي عميل يفشل ← الليلة تفشل. لا عتبة ولا أغلبية — بعميل واحد، قاعدة الأغلبية زر كتم.
- **`NO_MATCHES` و`SKIPPED_OPTED_OUT` تخرج بصفر**: يوم بلا فرص مطابقة هو البوابة تعمل؛ الإنذار عليه يدرّب المشغّل على تجاهل الإنذارات.
- **`"delivery": {}` ليست عطلًا** — تحقّق على ١٦ ليلة: عطلة الرياض · عميل خُدم اليوم · حزمة معلّقة. أُضيف `delivery_phase` و`delivery_unclosed` ليصير الصمت مقروءًا بدل أن يُعالَج بإنذار كاذب كل جمعة.
- **لا شيء في تحقق الإقلاع قاتل**: كل الشروط تعطّل مسار الطلب الجديد فقط، وكلها فاشلة-مغلقة أصلًا. رفض الإقلاع يحوّل «المبيعات متوقفة» إلى «المنتج ساقط لمن دفع» — أسوأ، ولقيمة يملكها المالك وحده.
- **إنذار تيليجرام من المؤقّت اليومي فقط**: العامل `Restart=always/5s`، فلو أنذر لوضع الإنذار على جواله اثنتي عشرة مرة في الدقيقة. الجانب الآخر من «إنذار لا يقرؤه أحد» هو إنذار أعلى من أن يُقرأ.
- **الرمز الحيّ لا يُعرض مرتين**: البديل إصدار عند الطلب لا استرجاع — لأننا نحفظ الهاش فقط، وهي خاصية تستحق البقاء. والتدوير حامل ثقل: رمزان حيّان يجعلان `scalar_one_or_none` ترمي داخل العامل وتقفل العميل خارج التفعيل.

## قرارات معمارية (تتمة)

- **سياسة الفحص معلَنة لا صامتة**: محرّك لا يستجيب ← **رفض** (تعطّله شيء يستطيع المهاجم أن يسببه)؛ لا محرّك ← **قبول موسوم `unscanned`** (رفض كل رفع يُسقط المنتج على من دفع لأجل ضابط لم يكن يعمل). الخيار المحذوف: تمرير صامت يقدّم نفسه كفحص. **هذا قرار منتج — يحتاج مدخلًا في CHANGELOG قبل النشر.**
- `scanner_health` **كلّية**: فاحص بلا `health()` يُقرأ أحمر، فلا يعود الصمت الأخضر.

## قرارات تنتظر فهد

- **انقسام مسار التخزين حادث الآن**: الحاوية تكتب `/data` والوحدات `/root/career/data`، وكلاهما فيه `tenants/`. النسخ يغطيهما **بالصدفة**. يحتاج قرارًا: مسار واحد، أم تثبيت التغطية عمدًا.

## مخاطر النشر وخطة الرجوع

- **لا Push ولا Deploy ولا Restart** حتى بوابة موافقة واحدة مجمّعة.
- قبل النشر: التحقق من آخر نسخة احتياطية واختبار استعادة.
- الرجوع: `git revert` + إعادة بناء الصورة من الكوميت السابق + إعادة تشغيل الخدمات بالترتيب.

## العوائق الخارجية

| العائق | المالك | ما يحجبه |
|---|---|---|
| موافقة API سلة + توكن صالح | فهد | شراء بريال، اختبار الاسترداد، ربط المنتجات الحية |
| اعتماد قوالب ميتا الخمسة | فهد | التسليم خارج نافذة ٢٤ ساعة |
| مراجعة بشرية لأول السير | فهد | بوابة الموجة |
| نصوص السياسات القانونية | فهد | نشر السياسات الثلاث |

## الكوميتات المحلية (لم تُدفع)

| الكوميت | المحتوى |
|---|---|
| `ac028b1` | سجل التغييرات §24 (وثائق فقط، سبق الكود) |
| `fea3423` | الموجة: ١٦ من P0 و٥ من P1 — ٤٩ ملفًا، ١٢٠١ اختبارًا |
| `591bcea` | موجة التصحيح بعد المراجعة الأولى — ١٢٣٧ اختبارًا |
| `b6948f3` | تسليم حالة عند انقطاع الحد |
| `0508695` | إصلاح انحدارَي المراجعة الثانية — **١٢٤٥ اختبارًا** |

## ما وجدته الجولة العدائية الأولى ولم يُصلَح بعد

A6 رابط التفعيل ما زال يصل محادثة ذات سجل دائم · A7 حارس الكاتب الواحد يفحص ملفًا لا جدولًا · A8 سقّاطة العزل تُفلَت بسطر، و`src/career_core/` خارج نطاق المسح · A9 `webhook_events` بلا RLS ودور التطبيق يقرأ أجسام رسائل كل المستأجرين · A13 اختبار انحراف البيئة أعمى عن حقل بلا alias · A14 ادعاء قفل الهجرة (الترقية كلها في معاملة واحدة) · A16 مِنصّة اختبار الفاحص لا تؤكد البايتات المُرسَلة.

## ⚙️ شرطان دائمان — لا يسقطان بانقطاع ولا بجلسة جديدة

**١) أكثر من إيجنت دائمًا.** لا يُنفَّذ عمل جوهري بإيجنت واحد ولا بالقائد وحده. كل موجة: ملكية ملفات **حصرية بلا تقاطع** · كل `pytest` عبر `flock /tmp/career_test.lock` · **إيجنت واحد فقط يضيف هجرات في الموجة** (هجرتان متزامنتان تصنعان رأسين متشعّبين) · ثم بوابة كاملة · ثم **مراجع عدائي يهاجم عمل المنفّذين** لا يراجعه.

**٢) الاستئناف من هذا الملف لا من الذاكرة.** يُحدَّث **قبل** إطلاق كل موجة، لا بعدها — فلو انقطع الحد في منتصفها، الجدول أدناه هو التكليف كاملًا. **الإيجنتات لا تستأنف تلقائيًا**: عند العودة، اقرأ `git log --oneline -12` و`git status`، ثم أعد إطلاق ما لم يُودَع عمله.

### الموجة الخامسة — سُجّلت قبل الإطلاق (٦ أغسطس، بعد `e5701b0`)

**سببها**: المراجع العدائي على التشغيل والوثائق أثبت **ثلاثة**: (١) لا شيء من عمل النبضة منشور، **والاختبارات تقرأ نسخة المستودع فلا ترى الانحراف أبدًا** · (٢) `Type=notify` فتح مسار صمت **جديدًا** عند انتهاء مهلة الإقلاع — أُصلح · (٣) الورقة تبيع **١٥ مقعدًا** والمشحون **٣٠**.

| # | الملكية الحصرية | المهمة |
|---|---|---|
| **S** | `scripts/alert_unit_failure.sh` · `tests/test_ops_watchdogs.py` · `ops/systemd/` · `scripts/verify_restore.sh` · `src/career/db/session.py` | حارس انحراف **المنشور مقابل المستودع** (هو ما كان سيمسك الحادثة) · عاصفة الإنذار ٢٨٣ رسالة/يوم تحتاج قرارًا · `connect_timeout` مفقود من كل اتصال |
| **T** | `README.md` · `docs/PROGRESS.md` · `tests/test_docs_truth.py` | أوامر مضمَّنة **لا تعمل على هذا الجهاز** · جدول هجرات متأخر برقمين · «سبع هجرات» وهي تسع · وثلاث ثغرات في الحارس: إعادة الصياغة تتجاوزه، وقائمة عبارات فارغة تنزع سلاحه، **وتعليق يُحتسب قراءة** |
| **U** | `docs/WHITEPAPER.html` · `docs/STORE-PAGES-AR.md` · `docs/DEVIATIONS.md` | `seats_cap` يبيع رقمًا خاطئًا **حيًّا** · وجرد ما يُباع بالعربية ولا يشحنه كود: لقب المؤسس · قروب المؤسسين · العدد على الصفحة · حق العدول · رد ٢٤ ساعة · الفرص القريبة · الشركات المحظورة |

| **V** | `salla/lifecycle.py` · `promises/guarantee.py` · `promises/career_session.py` · `telegram/console.py` · `tests/test_promises.py` · `tests/test_salla_lifecycle.py` | ثلاثة عيوب أثبتها المراجع الأول بفشل حقيقي — **وأخطرها صنعته الموجة الرابعة نفسها**: حارس «المحاسبة لا تعطّل الدورة» **لا يستطيع الانطلاق** لأن `record_out` لا يلمس القاعدة، فالإدراج يقع عند flush خارج الحارس، **فصفٌّ واحد فاسد يُلغي ليلةً كاملة من علامات دورة الحياة بعد إرسالها والدفع لها** · و`opted_out` ما زال لقطة بلا حدّ أدنى (الإصلاح طُبِّق على `paused` وحده) · وبطاقة المشغّل تعرض جلسة متأخرة **وزرُّها يقول لا يوجد طلب** |

### الموجة السادسة — سُجّلت قبل الإطلاق (٦ أغسطس، بعد بوابة الموجة الخامسة)

**سببها**: البوابة حمراء بفشلٍ **واحد صادق** — انحراف الجهاز. وهذا يكشف أن `gate.sh` تخلط سؤالين مختلفين: **«هل يصلح هذا الكود للشحن؟»** (سؤال المستودع) و**«هل الجهاز يشغّل ما يقوله المستودع؟»** (سؤال النشر). الأول يمنع الدفع، والثاني **لا يجوز أن يمنعه** — وإلا تجمّد الإيداع حتى النشر، **والضغط الحتمي عندئذٍ هو حذف الاختبار الصادق**.

| # | الملكية الحصرية | المهمة |
|---|---|---|
| **G** | `scripts/gate.sh` · `tests/test_ops_watchdogs.py` · `pyproject.toml` · `scripts/deploy_preflight.sh` (جديد إن لزم) | فصل البوابتين **بلا إضعاف الإنذار**: بوابة الشحن تفصل فحوص الجهاز الحيّة، **لكن تطبعها بوضوح ولا تبتلعها**، وبوابة ما قبل النشر تشترط خضرتها. **التخطّي الصامت ممنوع** — هو الباب الذي دخلت منه حادثة اليوم |
| **X** | مراجعة عدائية على عمل الموجة الخامسة كلها — **لا يملك ملفات، يفتح تقريرًا فقط** | الموجة الخامسة **لم تُراجَع عدائيًا بعد**: حارس AST الجديد · سجلّ الوعود · المعاملات المتداخلة · ختم منع التكرار · مهلة الاتصال · حارس الانحراف نفسه |

### الموجة السابعة — سُجّلت قبل الإطلاق (٦ أغسطس، بعد مراجعة الموجة الخامسة)

**سببها**: المراجع العدائي أثبت أن أقوى ادعاء في الموجة الخامسة **باطل**: حارس القراءة يُلتَفّ عليه بوحدة يتيمة لا تعمل. **ورفض المراجع ترقيعه** بحجّة أن سدّ العرض التوضيحي يترك الباب مفتوحًا **ويشتري ثقة كاذبة** — وهي الحجّة نفسها التي بُنيت عليها §٢٥.

| # | الملكية الحصرية | المهمة |
|---|---|---|
| **Y** | `tests/test_docs_truth.py` | إمّا وصولية حقيقية من خريطة الاستيراد، **وإمّا تُخفَّض التسمية ويُصرَّح بحدّها** — لا ثالث · وسجلّ الوعود يحرس أسطر القوائم وحدها، **وأقوى ادعاءات الصفحة خارجه أصلًا** |
| **Z** | `src/career/whatsapp/delivery.py` · `src/career/promises/guarantee.py` · `tests/test_whatsapp_delivery.py` · `tests/test_promises.py` | فحص قيمة المزوّد **عند القمع الواحد** لا عند موضع من ٢٢ — ويمسّ دلالة مطابقة الإيصالات فيُوثَّق القرار · وسقّاطة تفرض اقتران `opt_out_at` بسطر مصنَّف، فالصحّة اليوم **عُرف لا بناء** |
| **AA** | `scripts/alert_unit_failure.sh` · `tests/test_alert_dedup.py` (جديد) | قفزة الساعة إلى الوراء **تُسكِت** سكربتًا يعلن أنه يفشل مفتوحًا · وقراءة-تعديل-كتابة غير ذرّية تُفسد العدّاد الذي يقتبسه التذكير |

**ممنوع عليهم**: `scripts/gate.sh` · `tests/test_ops_watchdogs.py` · `pyproject.toml` — إيجنت فصل البوابتين يعمل عليها الآن.

### الموجة الثامنة — توكن سلة (سُجّلت قبل الإطلاق، ٧ أغسطس)

**سببها**: سلة وافقت، وطلب الوصول وصل متجر فهد وينتظر موافقته. و**اللحظة التي يوافق فيها هي اللحظة الوحيدة التي يُرسَل فيها التوكن** — والكود اليوم **يوسمها «متجاهَل»**. القرار الأمني مكتوب في §٢٩ قبل الكود.

| # | الملكية الحصرية | المهمة |
|---|---|---|
| **T1** | `src/career/salla/provisioning.py` · `src/career/salla/tokens.py` (جديد) · `tests/test_salla_tokens.py` (جديد) | استهلاك `app.store.authorize`: **التوقيع قبل أي قراءة** · حفظ في ملف البيئة بصلاحيات مغلقة · **صفر تسريب** في سجل أو قناة أو رسالة خطأ |
| **T2** | `scripts/refresh_salla_token.py` (جديد) · `ops/systemd/career-salla-token.*` (جديد) · `src/career/engine/cli.py` · `tests/test_salla_token_refresh.py` (جديد) | تجديد **قبل** الانتهاء بمؤقّت · وفحص إقلاع يقيس المتبقّي · وإنذار **قبل** التوقف لا بعده |

**الواجهة المتفق عليها بينهما** (T1 يكتبها، T2 يستهلكها):
`tokens.store_credentials(...)` · `tokens.current()` → `SallaCredentials(access_token, refresh_token, expires_at, store_id)` · `tokens.days_left()`

### الموجة التاسعة — سُجّلت قبل الإطلاق (٧ أغسطس، بعد أن صار الربط حيًّا)

| # | الملكية الحصرية | المهمة |
|---|---|---|
| **R1** | `src/career/db/session.py` · `tests/test_db_engine_guard.py` | السقّاطة تطلب استثناء دور لكل ما تشغّله systemd، **وسكربت التجديد لا يفتح اتصالًا بالقاعدة أصلًا** — فمنحه استثناءً يوسّع الامتياز بلا سبب. السؤال الصحيح: «هل يبني محركًا؟» لا «هل تشغّله systemd؟» |
| **R2** | مراجع عدائي — **لا يملك ملفات** | مسار اعتماد سلة كله (T1+T2+T3) **حيّ ولم يُراجَع**: التوقيع قبل القراءة · الكتابة الذرّية · هوية المتجر · استعمال رمز التجديد **مرة واحدة** · وصفر تسريب |

### الموجة العاشرة — عيوب المراجعة العدائية على مسار الاعتماد (سُجّلت قبل الإطلاق، ٧ أغسطس)

| # | الملكية الحصرية | المهمة |
|---|---|---|
| **F1** | `src/career/salla/provisioning.py` · `tests/test_salla_provisioning.py` | **C1 (الأخطر)**: حدث موقَّع بلا `merchant` يبلغ فرع UNCLAIMED **فيستبدل الاعتماد الحيّ ويرسل إنذارًا أخضر** · **C2**: رفض متجر أجنبي يُرسل المشغّل إلى «راجع مساحة القرص»، **ويتكرر بلا تصعيد** |
| **F2** | `src/career/engine/cli.py` · `src/career/webhooks/intake.py` · `src/career/salla/tokens.py` · `scripts/refresh_salla_token.py` · `tests/test_salla_tokens.py` · `tests/test_salla_token_refresh.py` | **C3**: `days_left=None` تعني «لا توكن» و«لا تاريخ» معًا، فيُؤمر المشغّل بإعادة تثبيت مدمّرة · **C4**: المكنسة تُتلف الاعتماد الذي وعد الرفض بحفظه · **C5**: `.bak` توكن حيّ بلا دورة حياة · **C6**: `repr` يحمل التوكنين |

### الموجة الحادية عشرة — سُجّلت قبل الإطلاق (٧ أغسطس، بكامل القوى)

| # | الملكية الحصرية | المهمة |
|---|---|---|
| **G1** | `scripts/consume_stored_authorize.py` · `tests/test_consume_authorize.py` (جديد) | قارئ المتجر في السكربت **يخالف قارئ العامل** بعد إصلاح F1 — فتقريره التمهيدي يتنبّأ بغير ما سيقع، وبوابته تختلف عن الكاتب الذي وُجدت لتتنبأ به |
| **G2** | `src/career/telegram/console.py` · `src/career/whatsapp/inbound.py` · `src/career/promises/career_session.py` · `tests/test_admin_console.py` | **مبنيّ ولا يُنادى**: اقتطاع الاسترداد الصحيح لا نداء إنتاجي له والبرج يطبع قيمة ثابتة **فيُنقص من حساب العميل** · ورسالة عميل لمّاح+ الحرّة **لا تبلغ المشغّل أبدًا**، فالباقية ٤٤٩ تعود «ما يأخذه ١٩٩ زائد نجمة» |
| **G3** | `src/career/whatsapp/activation_flow.py` · `tests/test_activation_flow.py` | من قال «إيقاف» ثم **اشترى من جديد** يُفعَّل ويستقبل الترحيب **ثم يُتخطّى كل ليلة إلى الأبد ولا شيء يخبره**. ولا يُمسح الإيقاف تلقائيًا — **تعليمة امتثال قائمة** |

### الموجة الثانية عشرة — عيوب المراجعة على موجة الوصل (سُجّلت قبل الإطلاق، ٧ أغسطس)

| # | الملكية الحصرية | المهمة |
|---|---|---|
| **H1** | `src/career/whatsapp/worker.py` · `src/career/promises/career_session.py` · `tests/test_whatsapp_worker.py` | **D1.1 (الأخطر)**: شرط `landed is None` يعني أن **أول رسالة بعد يوم صامت** — وهي التي تُنزل الحزمة المحجوزة — **لا تُصعَّد أبدًا** · بصمة صوتية لا تُصعَّد بينما المستند يُصعَّد بلا حجّة · جواب نتيجة **مكتوب** وضغطة **بطاقة قديمة** يفتحان تذكرة خاطئة · **وضغطة خاطئة تحرق المكان الوحيد** · و`inbound_message_id` مفقود · و`opted_out` **بلا قارئ** فالإشعار يُدفن · وقرب-الاستئناف يُقابَل بصمت تام |
| **H2** | `src/career/salla/provisioning.py` · `src/career/whatsapp/activation_flow.py` · `tests/test_salla_provisioning.py` · `tests/test_activation_flow.py` | **D2.1**: **التجديد غير مغطّى وهو الطريق الأشيع** — من يُسكِت ولا يلغي يُجدَّد شهورًا بلا إشعار ولا تذكرة ولا ليلة تفشل · و`ALREADY_LINKED` يدّعي أنه إعادة محاولة «تكلّمت وذكّرت» وهو غير صحيح إن جاء الإسكات بعد التفعيل · **وخطأ mypy عند 900** |
| **H3** | `scripts/consume_stored_authorize.py` · `tests/test_consume_authorize.py` | عشرة أخطاء أنواع، منها أن `__str__` **مكتوب حقلًا في NamedTuple** — فالإخفاء الذي يحمي التوكن **قد لا يعمل أصلًا**، وهذا يُفحص لا يُصلَح بالثقة |

| **H4** | `src/career/engine/cli.py` · `src/career/webhooks/intake.py` · `tests/test_engine_cli_exit.py` | **المضخّم**: أي استثناء في منتصف كنس دورة الحياة **يُلغي علامات كل عميل عولج تلك الليلة** — أُزيل الزناد ولم يُزَل المضخّم · و`signature_valid` **ثابت مكتوب لا حكم محسوب** فاسمه يَعِد بما لا يفعل · و«الاعتماد ما زال صالحًا» **محدود برقم يرسله طرف ثالث** |
| **H5** | `docs/DEVIATIONS.md` · `docs/STORE-PAGES-AR.md` · `docs/PROGRESS.md` · `README.md` | ثلاثة من «يُباع ولا يُشحن» **صارت موصولة اليوم** — والوثائق ما زالت تصفها معلَّقة، وكتلة الحجر فوق صفحة المتجر تحجر ما لم يعد محجورًا |

### الموجة الثالثة عشرة — ٦٣ سطرًا مخربطًا (سُجّلت قبل الإطلاق، ٧ أغسطس)

**القرار**: الحارس الجديد أحمر بحقّ. **رُفضت قائمة الأساس** — ٤٦ من الـ٦٣ في الشاشات والتقرير الأسبوعي، أي **في ما يقرؤه فهد نفسه كل أحد**. فتُصلَح لا تُسكَّت.

| # | الملكية الحصرية | العدد |
|---|---|---|
| **J1** | `src/career/telegram/views.py` · `src/career/telegram/weekly_report.py` · اختباراتهما | ٤٦ |
| **J2** | `src/career/whatsapp/worker.py` · `src/career/whatsapp/activation_flow.py` · `src/career/promises/career_session.py` · `src/career/cv/daily_run.py` · اختباراتها | ٧ |
| **J3** | `src/career/engine/quota.py` · `src/career/onboarding/retention.py` · `src/career/salla/lifecycle.py` · `src/career/telegram/console.py` · `scripts/run_admin_bot.py` · اختباراتها | ١٠ |

**ممنوع على الثلاثة**: `tests/test_alert_direction_purity.py` — الحارس نفسه لا يُعدَّل ليُخضِر شيئًا.

### الموجة الرابعة عشرة — البنود التي أبلغ عنها المنفّذون ولم يملكوها (سُجّلت قبل الإطلاق، ٧ أغسطس)

| # | الملكية الحصرية | المهمة |
|---|---|---|
| **K1** | `src/career/onboarding/orchestrator.py` · `src/career/onboarding/deps.py` (إن وُجد) · `tests/test_onboarding_orchestrator.py` | **سؤال قبل إصلاح**: `handle_enrichment` يرجع `False` من أول سطر حين `achievement_renderer is None` — **وهو الافتراضي المشحون**. فهل F-ENRICH كلها ميتة في الإنتاج؟ والجلسة تبقى مفتوحة، فالمحادثة ميتة والحالة باقية |
| **K2** | `src/career/cv/outcome_followup.py` · `src/career/telegram/console.py` · `tests/test_outcome_followup.py` | تذكرة مفتوحة منسيّة **تُسكِت خطّ ذلك العميل إلى الأجل** ولا شيء يكنسها · ومطابقة الجواب المكتوب موضعها `parse_answer` لا التفافٌ في العامل |
| **K3** | `docs/DEVIATIONS.md` · `docs/PROGRESS.md` · `README.md` · `tests/test_docs_truth.py` | وثائق **متأخرة في الاتجاه الآمن**: قيست حين كان خط لمّاح+ نصف مشحون وقد اكتمل بعدها · وسطر البطاقة يمكن أن يُسنَد إلى كود حقيقي الآن |

### الموجة الخامسة عشرة — ما بقي بعد النشر (سُجّلت قبل الإطلاق، ٧ أغسطس ليلًا)

**الحالة**: النشر تمّ · القاعدة `0028` · العامل حيّ يعالج · `preflight` أخضر · `origin/main` عند `0631887`.

| # | الملكية الحصرية | المهمة |
|---|---|---|
| **P1** | `src/career/promises/guarantee.py` · `ops/systemd/career-promises.*` (جديد) · `src/career/cv/daily_run.py` · اختباراتها | **ضمان ٧٢ ساعة يُقاس بكنسٍ يعمل ١١:٠٠ صباحًا** — فخرقٌ يقع ظهرًا ينتظر إلى صباح الغد: **قياسٌ متأخر بيوم عن الوعد نفسه**، والتعليق كان يقول ٤:٣٠ ولا مؤقّت بذلك الاسم |
| **P2** | `src/career/cv/outcome_followup.py` · `src/career/whatsapp/worker.py` · اختباراتهما | `pending_job_ref` صار يعمل **على كل رسالة واردة** وهو 1+N — عميلٌ بمئة تقديم يكلّف مئة استعلام لكل رسالة · و`_channel_for_phone` في العامل **يقارن الجوال حرفيًّا** بينما جاراه يستعملان `phone_variants` — نفس حدّ الـ`+` الذي لدغ من قبل |
| **P3** | `scripts/verify_restore.sh` · `scripts/consume_stored_authorize.py` · اختباراتهما | تعليقٌ يدّعي أن التهجئة غير المعروفة «تسقط إلى فرع لا يجزم» **والكود يسقط إلى الفرع المتّهِم** · و`Offered._asdict()` والتقطيع **يعرضان التوكنين** ولا يمنعهما درع |

### الموجة السادسة عشرة — الأوقات والقوالب والتحقق الشامل (سُجّلت قبل الإطلاق، ٧ أغسطس ليلًا)

**قياس حيّ جديد**: قوالب ميتا **كلها APPROVED** — العائق المسجَّل على فهد **مغلق**. لكن **سبعة من ثمانية عربية مصنَّفة MARKETING**، ومنها `daily_opportunities_utility` الذي اسمه يقول غير تصنيفه.

| # | الملكية الحصرية | المهمة |
|---|---|---|
| **Q1** | `ops/systemd/*.timer` · `src/career/whatsapp/window.py` · `scripts/run_worker_loop.py` · اختباراتها | **جرد كل وتيرة في النظام** واشتقاق الصحيح من الوعد الذي تقيسه أو الحدث الذي تلاحقه — لا من العادة. كل رقم يُبرَّر أو يُغيَّر |
| **Q2** | `src/career/whatsapp/templates.py` (أو ما يعادله) · `src/career/whatsapp/delivery.py` · اختباراتهما | **تصنيف القوالب**: خدمةٌ اشتراها عميل ليست تسويقًا — أغلى، ويخضع لحدود وخنق، **وقد لا يصل لمن أوقف التسويق**. جرد ما يُرسَل متى وبأي تصنيف، والفرق بين المسمّى والمصنَّف |
| **Q3** | مراجع عدائي — **لا يملك ملفات** | التحقق الشامل من إصلاحات اليوم: الأوقات الجديدة · القفلان · الاستعلام الواحد · حدّ الجوال · الصنف المشدود · فروع فحص الأحد |

**ملاحظة للمراجعة القادمة**: `refund_deduction_sar` و`breach_notice_ar` و`record_suppression` **بلا نداء إنتاجي واحد** — فما وُصف بأنه إصلاح مالٍ خطير كان في شيفرة لا يبلغها أحد. **التحقق من وجود نداء حيّ يسبق تقدير الخطورة.**

### الموجة الرابعة (اكتملت — أُطلقت ٦ أغسطس بعد `566b935`)

| # | الملكية الحصرية | المهمة |
|---|---|---|
| **M** | `salla/lifecycle.py` · `salla/provisioning.py` · `salla/renewal.py` · `onboarding/privacy.py` · `telegram/console.py` · `db/models.py` · `db/session.py` · **`migrations/` (وحده)** | فوترة القوالب غير المرئية (١٠٠٪ غير محتسَب لطلب لم يُفعَّل) · وصل سجل التدقيق للأربعة المستحقة · أرقام المال في البرج · إسقاط جدولَي الطابور |
| **W** | `scripts/run_worker_loop.py` · `scripts/run_admin_bot.py` · `scripts/alert_unit_failure.sh` · `scripts/post_boot_report.sh` · `ops/systemd/` · `career/notify.py` (جديد) | نبضة حياة — العامل الميت الذي تراه systemd حيًّا. **الترتيب إلزامي: الكود قبل ملف الوحدة**، والنبضة في نهاية الدورة لا في `finally` |
| **R** | `engine/ranking.py` · `cv/daily_run.py` · `whatsapp/inbound.py` · `promises/career_session.py` · `promises/guarantee.py` | عطل صحّة يختبئ كفهرس مفقود (كبت المجموعة ينهار مع معاملات التتبّع) · وصلتان · **ومهاجمة وحدتَي الوعود** |
| **C** | `cv/template.py` · `cv/render.py` · `README.md` · `pyproject.toml` · `docs/WHITEPAPER.html` · `docs/DEVIATIONS.md` · `docs/PROGRESS.md` | مستند يحجز حذف D23 · مزامنة السجل · توسيع حارس الورقة |

**بعد الموجة**: بوابة كاملة → مراجع عدائي → إيداع محلي → دفع. **لا نشر** (محجوز).

## 🔄 موجة «كل شيء عدا سلة» — ٦ أغسطس

**القرار**: سلة معلَّقة على اجتماعهم. كل ما عداها يُبنى حتى الجاهزية الكاملة.

**المُسند الآن (أربعة إيجنتات متوازية، ملكية ملفات حصرية):**

| الإيجنت | الملكية | المهمة |
|---|---|---|
| **P** | `db/models.py` · `migrations/` · `salla/renewal.py` · `salla/subscriptions.py` · `cv/daily_run.py` · `telegram/console.py` · `telegram/views.py` | الوعود الثلاثة + الفهارس |
| **D** | `queue/` · `worker/` · `audit.py` · `cv/publish.py` · `cv/render.py` · `whatsapp/activation.py` · `scripts/verify_restore.sh` · `ops/systemd/` · `docs/DEVIATIONS.md` | الأنظمة السبعة الميتة — قرار موثّق لكل واحد |
| **A** | `career/arabic.py` (جديد) · `onboarding/achievement_render.py` · `onboarding/extraction.py` · `whatsapp/inbound.py` · `onboarding/orchestrator.py` · `funnel/flow.py` | نقل التطبيع العربي إلى وحدة ورقية |
| **Q** | `cv/close.py` · `telegram/weekly_report.py` · `README.md` · `docs/PROGRESS.md` · `docs/WHITEPAPER.html` | P2 + مزامنة الوثائق مع الواقع |

**قواعد ثابتة لكل إيجنت**: ملكية حصرية بلا تقاطع · كل `pytest` عبر `flock /tmp/career_test.lock` · لا كوميت ولا دفع ولا إعادة تشغيل · لا كتابة في قاعدة الإنتاج-التجريبي · لا ترحيل عليها · لا طباعة رقم أو رمز أو حمولة.

**عند عودة الحد**: اقرأ `git log --oneline -12` و`git status`، ثم أكمل من جدول P0/P1 أدناه ومن جدول الإيجنتات أعلاه. **الإيجنتات لا تستأنف تلقائيًا** — أعد إطلاق ما لم يُودَع عمله.

## معلَّق على فهد (لا يُبنى)

- **ترحيل قاعدة الإنتاج-التجريبي** إلى الرأس — البيئة تحجبه عني، والكود المدفوع يعتمد عليه. **لا نشر قبله.**
- **سلة**: الاجتماع · المنتجات الثلاثة · الشراء بريال · اختبار الاسترداد · **والتوكن منتهٍ منذ ٢٩ يوليو بلا مسار تجديد**.
- **قوالب ميتا** الخمسة.
- **الوعود الثلاثة**: صيغة الضمان والجلسة وقفل السعر تجاريًّا.

## أول خطوة عند الاستئناف

**اقرأ كتلة «⛔ عاجل» في أول هذا الملف قبل أي شيء** — هناك حادثة حيّة مفتوحة تنتظر إذن فهد بأمر واحد.

ثم بالترتيب:

1. `git log --oneline -12` و`git status` — اعرف ما أُودع وما لم يُودَع.
2. المراجعان العدائيان على الموجة الرابعة (صحّة الكود · التشغيل والوثائق) — إن لم تصل نتيجتهما، **أعد إطلاقهما**؛ الإيجنتات لا تستأنف تلقائيًا.
3. أصلح ما يثبتانه، ثم بوابة كاملة عبر `flock /tmp/career_test.lock bash scripts/gate.sh`.
4. إيداع محلي ثم دفع. **لا نشر** — النشر محجوز على بوابة موافقة واحدة مجمّعة من فهد.

**ترتيب النشر حين يُؤذَن به** (ليس تفضيلًا — `Type=notify` على كودٍ لا يقول `READY=1` عاملٌ تقتله systemd عند الإقلاع ثم تعيد قتله):

```
الهجرات أولًا  →  ملفات الكود  →  ملفات الوحدات  →  daemon-reload  →  restart  →  إثبات من السجل
```

**مسؤول الاختبارات وحده يشغّل البوابة**، وكل `pytest` عبر `flock /tmp/career_test.lock` — **بهذا المسار حرفيًا**؛ إيجنت استعمل اسمًا آخر هذا اليوم فتسابق مع غيره على قاعدة الاختبار.
