# ابدأ من هنا — تشغيل المشروع بكلود كود

هذا الملف هو كل اللي تسويه بيدك. أي شي غير مذكور هنا = كلود كود يسويه.

---

## الخطوة 1 — ثبّت كلود كود على السيرفر (مرة واحدة)

من جهازك، ادخل السيرفر:

```
ssh root@169.58.13.217
```

ثم على السيرفر:

```
apt update && apt install -y git tmux curl
curl -fsSL https://claude.ai/install.sh | bash
```

أعد فتح الجلسة أو نفّذ `source ~/.bashrc` ثم تأكد:

```
claude --version
claude doctor
```

## الخطوة 2 — سجّل دخول كلود كود

```
tmux new -s career
mkdir -p /root/career && cd /root/career
claude
```

أول تشغيل بيعطيك رابط تسجيل دخول — انسخ الرابط وافتحه في متصفح جوالك أو جهازك، سجّل بحساب Claude حقك، والصق الكود الراجع في التيرمنال. (tmux عشان الجلسات الطويلة ما تنقطع لو سكّرت الاتصال — ترجع لها بـ `tmux attach -t career`).

## الخطوة 3 — ارفع ملفات الحوكمة (مرة واحدة)

نزّل من هذه المحادثة: `WHITEPAPER.html` (سلّمتك إياه سابقًا) + `CLAUDE.md` + `CHANGELOG-v1.1.md`. ثم من جهازك (مو من السيرفر):

```
scp CLAUDE.md root@169.58.13.217:/root/career/
scp WHITEPAPER.html CHANGELOG-v1.1.md root@169.58.13.217:/root/career/docs/
```

(لو مجلد docs مو موجود: `ssh root@169.58.13.217 "mkdir -p /root/career/docs"` أولًا).

## الخطوة 4 — جهّز GitHub (مرة واحدة)

أنشئ من متصفحك مستودع **خاص (Private)** فاضي على GitHub باسم مثل `career-platform`. بعدها على السيرفر:

```
apt install -y gh
gh auth login
```

واتبع الخطوات (نفس أسلوب رابط + كود). بعدها كلود كود يقدر يعمل push بنفسه.

## الخطوة 5 — الصق أول برومبت في كلود كود

افتح `claude` داخل `/root/career` والصق هذا حرفيًا:

> اقرأ CLAUDE.md ثم docs/WHITEPAPER.html ثم docs/CHANGELOG-v1.1.md كاملة قبل أي شيء.
> بعدها نفّذ المرحلة C1 من الورقة: هيّئ المستودع (git init، بنية مجلدات المشروع، .gitignore، .env.example، README مختصر، pyproject بإعداد Python 3.11 + FastAPI + SQLAlchemy + Alembic)، وأنشئ docs/PROGRESS.md وسجّل فيه الحالة، ثم اربط remote المستودع الخاص على GitHub واعمل أول push.
> ثم ابدأ C2: ملف docker-compose لبيئتين منفصلتين تمامًا (staging وproduction: PostgreSQL 16 + Redis لكل بيئة، أسرار وقواعد منفصلة)، وخدمة FastAPI فيها healthcheck، وهيكل StorageAdapter، وأساس RLS مع أول migration، واختبار cross-tenant أولي.
> اعرض عليّ خطة قصيرة أولًا قبل التنفيذ، ونفّذ خطوة خطوة، وحدّث PROGRESS.md، وفي النهاية قيّم شروط الخروج لكل مرحلة كما في الورقة قسم 13.

## الخطوة 6 — وش بيطلبه منك كلود كود يدويًا (كل اللي عليك مستقبلًا)

- **مفاتيح وأسرار** وقت الحاجة: مفتاح Anthropic API (للمحرك نفسه، غير حساب كلود كود)، مفتاح SerpApi، وبيانات سلة وواتساب في مراحلها — تلصقها في `.env` لما يطلبها.
- **نماذج الويب**: تعبئة تطبيق سلة في portal.salla.partners (بوجّهك حقل حقل وقتها)، وإعداد Meta/واتساب من لوحاتهم.
- **قرارات الورقة**: أي شي كلود كود يقول "يحتاج قرار منتج" — يرجع لك.
- أي شي ثاني: كلود كود.

## ترتيب المراحل بعد كذا

C1–C2 (اللي فوق) → C3 سلة → C4 واتساب → C5 الإعداد → C6 المحرك → C7 الـCV والتسليم → C8 تحليل الـCV → C9 الأصدقاء. كل مرحلة: تلصق برومبت "نفّذ المرحلة CX كما في الورقة" وكلود كود يقرأ التفاصيل من WHITEPAPER بنفسه.
